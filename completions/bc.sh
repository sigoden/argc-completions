#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @meta combine-shorts
# @flag -h --help           print this usage and exit
# @flag -i --interactive    force interactive mode
# @flag -l --mathlib        use the predefined math routines
# @flag -q --quiet          don't print initial banner
# @flag -s --standard       non-standard bc constructs are errors
# @flag -w --warn           warn about non-standard bc constructs
# @flag -v --version        print version information and exit
# @arg file*

command eval "$(argc --argc-eval "$0" "$@")"