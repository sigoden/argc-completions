#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -c                      use the complement of SET1
# @flag -C                      use the complement of SET1
# @flag --complement            use the complement of SET1
# @flag -d --delete             delete characters in SET1, do not translate
# @flag -s --squeeze-repeats    replace each sequence of a repeated character that is listed in the last specified SET, with a single occurrence of that character
# @flag -t --truncate-set1      first truncate SET1 to length of SET2
# @flag --help                  display this help and exit
# @flag --version               output version information and exit
# @arg set1
# @arg set2

command eval "$(argc --argc-eval "$0" "$@")"