#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -b --bell                     ring terminal bell changes between updates
# @flag -d --differences              highlight changes between updates
# @flag -s --skip-empty-diffs         skip snapshots with no changes (+0 -0) in history
# @option -n --interval <interval>    seconds to wait between updates (default "2s")
# @flag -p --precise                  attempt run command in precise intervals
# @flag -c --clockwork                run command in precise intervals forcibly
# @flag -t --no-title                 turn off header
# @flag --shell                       shell (default "sh")
# @flag --shell-options               additional shell options
# @flag --unfold                      unfold command result
# @flag --pty                         run on pty (experimental, not for Windows)
# @flag -h --help                     display this help and exit
# @flag -v --version                  output version information and exit
# @arg command

command eval "$(argc --argc-eval "$0" "$@")"