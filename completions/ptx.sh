#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -A --auto-reference                output automatically generated references
# @flag -G --traditional                   behave more like System V 'ptx'
# @option -F --flag-truncation <STRING>    use STRING for flagging line truncations.
# @option -M --macro-name <STRING>         macro name to use instead of 'xx'
# @flag -O                                 generate output as roff directives
# @flag -R --right-side-refs               put references at right, not counted in -w
# @option -S --sentence-regexp <REGEXP>    for end of lines or end of sentences
# @flag -T                                 generate output as TeX directives
# @option -W --word-regexp <REGEXP>        use REGEXP to match each keyword
# @option -b --break-file <FILE>           word break characters in this FILE
# @flag -f --ignore-case                   fold lower case to upper case for sorting
# @option -g --gap-size <NUMBER>           gap size in columns between output fields
# @option -i --ignore-file <FILE>          read ignore word list from FILE
# @option -o --only-file <FILE>            read only word list from this FILE
# @flag -r --references                    first field of each line is a reference
# @flag -t --typeset-mode                  - not implemented -
# @option -w --width <NUMBER>              output width in columns, reference excluded
# @flag --help                             display this help and exit
# @flag --version                          output version information and exit
# @option --format[roff|tex]
# @arg files*

command eval "$(argc --argc-eval "$0" "$@")"