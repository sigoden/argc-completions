#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -a --alternative           use the alternative dictionary
# @flag -d --alphanum              compare only blanks and alphanumeric characters
# @flag -f --ignore-case           ignore case differences when comparing
# @option -t --terminate <char>    define the string-termination character
# @flag -h --help                  display this help
# @flag -V --version               display version
# @arg string!
# @arg file*

command eval "$(argc --argc-eval "$0" "$@")"