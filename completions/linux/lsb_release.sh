#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -h --help           show this help message and exit
# @flag -v --version        show LSB modules this system supports
# @flag -i --id             show distributor ID
# @flag -d --description    show description of this distribution
# @flag -r --release        show release number of this distribution
# @flag -c --codename       show code name of this distribution
# @flag -a --all            show all of the above information
# @flag -s --short          show requested information in short format

command eval "$(argc --argc-eval "$0" "$@")"