#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -x --mountpoints    show mount point directories with a 'D'
# @flag -m --modes          show the mode bits of each file
# @flag -o --owners         show owner and group name of each file
# @flag -l --long           use a long listing format (-m -o -v)
# @flag -n --nosymlinks     don't follow symlinks
# @flag -v --vertical       vertical align of modes and owners
# @flag -h --help           display this help
# @flag -V --version        display version
# @arg pathname+

command eval "$(argc --argc-eval "$0" "$@")"