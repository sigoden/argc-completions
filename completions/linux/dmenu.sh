#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @meta combine-shorts
# @flag -b                 dmenu appears at the bottom of the screen.
# @flag -f                 dmenu grabs the keyboard before reading stdin if not reading from a tty.
# @flag -i                 dmenu matches menu items case insensitively.
# @option -l <lines>       dmenu lists items vertically, with the given number of lines.
# @option -m <monitor>     dmenu is displayed on the monitor number supplied.
# @option -p <prompt>      defines the prompt to be displayed to the left of the input field.
# @option -fn <font>       defines the font or font set used.
# @option -nb <color>      defines the normal background color.
# @option -nf <color>      defines the normal foreground color.
# @option -sb <color>      defines the selected background color.
# @option -sf <color>      defines the selected foreground color.
# @flag -v                 prints version information to stdout, then exits.
# @option -w <windowid>    embed into windowid.

command eval "$(argc --argc-eval "$0" "$@")"