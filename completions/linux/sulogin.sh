#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -p --login-shell            start a login shell
# @option -t --timeout <seconds>    max time to wait for a password (default: no limit)
# @flag -e --force                  examine password files directly if getpwnam(3) fails
# @flag -h --help                   display this help
# @flag -V --version                display version
# @arg file

command eval "$(argc --argc-eval "$0" "$@")"