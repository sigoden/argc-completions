#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag --help           Show this help
# @flag -H --halt        Halt the machine
# @flag -P --poweroff    Power-off the machine
# @flag -r --reboot      Reboot the machine
# @flag -h               Equivalent to --poweroff, overridden by --halt
# @flag -k               Don't halt/power-off/reboot, just send warnings
# @flag --no-wall        Don't send wall message before halt/power-off/reboot
# @flag -c               Cancel a pending shutdown

command eval "$(argc --argc-eval "$0" "$@")"