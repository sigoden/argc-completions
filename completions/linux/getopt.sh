#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -a --alternative                          allow long options starting with single -
# @option -l --longoptions <longopts>             the long options to be recognized
# @option -n --name <progname>                    the name under which errors are reported
# @option -o --options <optstring>                the short options to be recognized
# @flag -q --quiet                                disable error reporting by getopt(3)
# @flag -Q --quiet-output                         no normal output
# @option -s --shell[bash|csh|tcsh|sh] <shell>    set quoting conventions to those of <shell>
# @flag -T --test                                 test for getopt(1) version
# @flag -u --unquoted                             do not quote the output
# @flag -h --help                                 display this help
# @flag -V --version                              display version
# @arg optstring!
# @arg parameters+

command eval "$(argc --argc-eval "$0" "$@")"