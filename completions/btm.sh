#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag --autohide_time                          Automatically hides the time scale in graphs after being shown for a brief moment when zoomed in/out.
# @flag -b --basic                               Hides graphs and uses a more basic look.
# @flag --battery                                Shows the battery widget in default or basic mode.
# @flag -S --case_sensitive                      When searching for a process, enables case sensitivity by default.
# @flag -c --celsius                             Sets the temperature type to Celsius.
# @option --color[default|default-light|gruvbox|gruvbox-light|nord|nord-light] <COLOR SCHEME>  Use a pre-defined color scheme.
# @option -C --config <CONFIG PATH>              Sets the location of the config file.
# @flag -u --current_usage                       Sets process CPU% usage to be based on the current system CPU% usage rather than total CPU usage.
# @option -t --default_time_value <TIME>         Default time value for graphs.
# @option --default_widget_count <INT>           Sets the n'th selected widget type to use as the default widget.
# @option --default_widget_type <WIDGET TYPE>    Sets which widget type to use as the default widget.
# @flag --disable_advanced_kill                  Hides advanced options to stop a process on Unix-like systems.
# @flag --disable_click                          Disables mouse clicks from interacting with the program.
# @flag -m --dot_marker                          Uses a dot marker for graphs as opposed to the default braille marker.
# @flag --enable_cache_memory                    Enable collecting and displaying cache and buffer memory.
# @flag --enable_gpu_memory                      Enable collecting and displaying GPU memory usage.
# @flag -e --expanded                            Expand the default widget upon starting the app.
# @flag -f --fahrenheit                          Sets the temperature type to Fahrenheit.
# @flag -g --group                               Groups processes with the same name by default.
# @flag -a --hide_avg_cpu                        Hides the average CPU usage from being shown.
# @flag --hide_table_gap                         Hides the spacing between table headers and entries.
# @flag --hide_time                              Completely hides the time scale from being shown.
# @flag -k --kelvin                              Sets the temperature type to Kelvin.
# @flag -l --left_legend                         Puts the CPU chart legend to the left side rather than the right side.
# @flag --mem_as_value                           Defaults to showing process memory usage by value.
# @flag --network_use_binary_prefix              Displays the network widget with binary prefixes (i.e.
# @flag --network_use_bytes                      Displays the network widget using bytes.
# @flag --network_use_log                        Displays the network widget with a log scale.
# @flag --process_command                        Show processes as their commands by default in the process widget.
# @option -r --rate <TIME>                       Sets the data refresh rate.
# @flag -R --regex                               When searching for a process, enables regex by default.
# @option --retention <TIME>                     How much data is stored at once in terms of time.
# @flag --show_table_scroll_position             Shows the list scroll position tracker in the widget title for table widgets.
# @option -d --time_delta <TIME>                 The amount of time changed when zooming in/out.
# @flag -T --tree                                Defaults to showing the process widget in tree mode.
# @flag -n --unnormalized_cpu                    Shows all process CPU% usage without averaging over the number of CPU cores in the system.
# @flag --use_old_network_legend                 DEPRECATED - uses an older (pre-0.4), separate network widget legend.
# @flag -V --version                             Prints version information.
# @flag -W --whole_word                          When searching for a process, return results that match the entire query by default.
# @flag -h --help                                Print help (see a summary with '-h')

command eval "$(argc --argc-eval "$0" "$@")"