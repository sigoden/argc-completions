#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag --help                         Print usage and exit
# @flag --version                      Print version and exit
# @flag --verbose                      Print information on progress
# @option --default-packages <LIST>    Attach these packages on startup; a comma-separated LIST of package names, or 'NULL'
# @flag --save                         Do save workspace at the end of the session
# @flag --no-environ                   Don't read the site and user environment files
# @flag --no-site-file                 Don't read the site-wide Rprofile
# @flag --no-init-file                 Don't read the user R profile
# @flag --restore                      Do restore previously saved objects at startup
# @flag --vanilla                      Combine --no-save, --no-restore, --no-site-file, --no-init-file and --no-environ
# @flag -e                             R expression to run
# @arg file
# @arg args*

command eval "$(argc --argc-eval "$0" "$@")"