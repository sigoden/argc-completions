#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -f                     print the fat headers
# @flag -a                     print the archive header
# @flag -h                     print the mach header
# @flag -l                     print the load commands
# @flag -L                     print shared libraries used
# @flag -D                     print shared library id name
# @flag -t                     print the text section (disassemble with -v)
# @flag -x                     print all text sections (disassemble with -v)
# @option -p <routine name>    start dissassemble from routine name
# @option -s <segname+>        print contents of section
# @flag -d                     print the data section
# @flag -o                     print the Objective-C segment
# @flag -r                     print the relocation entries
# @flag -S                     print the table of contents of a library (obsolete)
# @flag -T                     print the table of contents of a dynamic shared library (obsolete)
# @flag -M                     print the module table of a dynamic shared library (obsolete)
# @flag -R                     print the reference table of a dynamic shared library (obsolete)
# @flag -I                     print the indirect symbol table
# @flag -H                     print the two-level hints table (obsolete)
# @flag -G                     print the data in code table
# @flag -v                     print verbosely (symbolically) when possible
# @flag -V                     print disassembled operands symbolically
# @flag -c                     print argument strings of a core file
# @flag -X                     print no leading addresses or headers
# @flag -m                     don't use archive(member) syntax
# @flag -B                     force Thumb disassembly (ARM objects only)
# @flag -q                     use llvm's disassembler (the default)
# @flag -Q                     use otool(1)'s disassembler
# @option -mcpu <arg>          use `arg' as the cpu for disassembly
# @flag -j                     print opcode bytes
# @flag -P                     print the info plist section as strings
# @flag -C                     print linker optimization hints
# @flag --version              print the version of /Applications/Xcode.app/Contents/Developer/Toolchains/XcodeDefault.xctoolchain/usr/bin/otool
# @arg object-file+ <object file>

command eval "$(argc --argc-eval "$0" "$@")"