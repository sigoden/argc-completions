#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -attr                   -n : fetch specific attribute
# @flag -raw                    -r : don't print attribute names before values
# @option -m -nullMarker <:>    substitute this string for null attributes in raw mode
# @flag -plist                  -p : output attributes in XML format to file.
# @flag -sdb                    -s : implies -attr _kMDItemSDBInfoexample:  mdls  ~/Pictures/Birthday.jpg
# @arg path

command eval "$(argc --argc-eval "$0" "$@")"