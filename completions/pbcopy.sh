#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @option -pboard[general|ruler|find|font]    specifies which pasteboard to copy to or paste from.
# @option -Prefer[txt|rtf|ps]                 tells pbpaste what type of data to look for in the pasteboard first.

command eval "$(argc --argc-eval "$0" "$@")"