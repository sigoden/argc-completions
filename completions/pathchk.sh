#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -p               check for most POSIX systems
# @flag -P               check for empty names and leading "-"
# @flag --portability    check for all POSIX systems (equivalent to -p -P)
# @flag --help           display this help and exit
# @flag --version        output version information and exit
# @arg files*

command eval "$(argc --argc-eval "$0" "$@")"