#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -c --commit     open current commit
# @flag -i --issue      open issues page
# @flag -s --suffix*    append this suffix
# @flag -p --print      just print the url

command eval "$(argc --argc-eval "$0" "$@")"