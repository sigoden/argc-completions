#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag --branches                   process all branches
# @flag --tags                       process all tags
# @flag --remotes                    process all remote-tracking branches
# @flag -v --verbose                 report all statistics, whether concerning or not
# @option --threshold <threshold>    minimum level of concern (i.e., number of stars) that should be reported (default --threshold=1)
# @flag --critical                   only report critical statistics
# @option --names <style>            display names of large objects in the specified style:
# @flag -j --json                    output results in JSON format
# @option --json-version <uint>      JSON format version to output (1 or 2) (default 1)
# @flag --progress                   report progress to stderr (default true)
# @flag --version                    report the git-sizer version number
# @flag --no-progress                suppress progress output

command eval "$(argc --argc-eval "$0" "$@")"