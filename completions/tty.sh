#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -s           print nothing, only return an exit status
# @flag --silent     print nothing, only return an exit status
# @flag --quiet      print nothing, only return an exit status
# @flag --help       display this help and exit
# @flag --version    output version information and exit

command eval "$(argc --argc-eval "$0" "$@")"