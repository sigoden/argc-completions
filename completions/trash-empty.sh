#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -h --help                              show this help message and exit
# @option --print-completion[bash|zsh|tcsh]    print shell completion script
# @flag --version                              show program's version number and exit
# @flag -v --verbose                           list files that will be deleted
# @option --trash-dir <TRASH_DIR>              specify the trash directory to use
# @flag --all-users                            empty all trashcan of all the users
# @flag -i --interactive                       ask before emptying trash directories
# @flag -f                                     don't ask before emptying trash directories
# @flag --dry-run                              show which files would have been removed
# @arg days*

command eval "$(argc --argc-eval "$0" "$@")"