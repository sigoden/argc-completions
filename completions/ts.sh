#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -r    convert existing timestamps to relative times
# @flag -i    print incremental timestamps since ast timestamp
# @flag -s    print incremental timestamps since start of program
# @flag -m    use monotonic clock
# @arg format

command eval "$(argc --argc-eval "$0" "$@")"