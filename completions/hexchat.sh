#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -h --help               Show help options
# @flag --help-all              Show all help options
# @flag --help-gtk              Show GTK+ Options
# @flag -a --no-auto            Don't auto connect to servers
# @option -d --cfgdir <PATH>    Use a different config directory
# @flag -n --no-plugins         Don't auto load any plugins
# @flag -p --plugindir          Show plugin/script auto-load directory
# @flag -u --configdir          Show user config directory
# @option -c --command          Execute command:
# @flag -e --existing           Open URL or execute command in an existing HexChat
# @option --minimize <level>    Begin minimized.
# @flag -v --version            Show version information
# @option --display             X display to use
# @arg url

command eval "$(argc --argc-eval "$0" "$@")"