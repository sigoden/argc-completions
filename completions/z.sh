#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @meta combine-shorts
# @flag -c    restrict matches to subdirectories of the current directory
# @flag -e    echo the best match, don't cd
# @flag -h    show a brief help message
# @flag -l    list only
# @flag -r    match by rank only
# @flag -t    match by recent access only
# @flag -x    remove the current directory from the datafile

command eval "$(argc --argc-eval "$0" "$@")"