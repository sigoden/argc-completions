#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag --batch                         do not do interactive display; implies -q
# @option --chdir <DIR>                 change to directory DIR
# @option --daemon <NAME>               start a (named) server in the background
# @option --bg-daemon <NAME>            start a (named) server in the background
# @option --fg-daemon <NAME>            start a (named) server in the foreground
# @flag --debug-init                    enable Emacs Lisp debugger for init file
# @option -d --display                  use X server DISPLAY
# @flag --module-assertions             assert behavior of dynamic modules
# @option --dump-file <FILE>            read dumped state from FILE
# @option --sandbox <FILE>              read Seccomp BPF filter from FILE
# @flag --no-build-details              do not add build details such as time stamps
# @flag --no-desktop                    do not load a saved desktop
# @flag -q --no-init-file               load neither ~/.emacs nor default.el
# @flag --no-loadup                     do not load loadup.el into bare Emacs
# @flag -nl                             do not load loadup.el into bare Emacs
# @flag --no-site-file                  do not load site-start.el
# @flag --no-x-resources                do not load X resources
# @flag --no-site-lisp                  do not add site-lisp directories to load-path
# @flag -nsl                            do not add site-lisp directories to load-path
# @flag --no-splash                     do not display a splash screen on startup
# @flag --no-window-system              do not communicate with X, ignoring $DISPLAY
# @flag -nw                             do not communicate with X, ignoring $DISPLAY
# @flag -Q --quick                      equivalent to: -q --no-site-file --no-site-lisp --no-splash --no-x-resources
# @option --script <FILE>               run FILE as an Emacs Lisp script
# @option -t --terminal <DEVICE>        use DEVICE for terminal I/O
# @option -u --user                     load ~USER/.emacs instead of your own
# @option -L --directory <DIR>          prepend DIR to load-path (with :DIR, append DIR)
# @option --eval <EXPR>                 evaluate Emacs Lisp expression EXPR
# @option --execute <EXPR>              evaluate Emacs Lisp expression EXPR
# @option --file                        visit FILE
# @option --find-file <FILE>            visit FILE
# @option -f --funcall <FUNC>           call Emacs Lisp function FUNC with no arguments
# @option --insert <FILE>               insert contents of FILE into current buffer
# @flag --kill                          exit without asking for confirmation
# @option -l --load <FILE>              load Emacs Lisp FILE using the load function
# @option --visit <FILE>                visit FILE
# @option --background-color <COLOR>    window background color
# @option -bg <COLOR>                   window background color
# @flag -D --basic-display              disable many display features; used for debugging Emacs
# @option --border-color <COLOR>        main border color
# @option -bd <COLOR>                   main border color
# @option --border-width <WIDTH>        width of main border
# @option -bw <WIDTH>                   width of main border
# @option --color <MODE>                override color mode for character terminals; MODE defaults to `auto', and can also be `never', `always', or a mode name like `ansi8'
# @option --cursor-color <COLOR>        color of the Emacs cursor indicating point
# @option -cr <COLOR>                   color of the Emacs cursor indicating point
# @option --font <FONT>                 default font; must be fixed-width
# @option -fn <FONT>                    default font; must be fixed-width
# @option --foreground-color <COLOR>    window foreground color
# @option -fg <COLOR>                   window foreground color
# @flag --fullheight                    make the first frame high as the screen
# @flag -fh                             make the first frame high as the screen
# @flag --fullscreen                    make the first frame fullscreen
# @flag -fs                             make the first frame fullscreen
# @flag --fullwidth                     make the first frame wide as the screen
# @flag -fw                             make the first frame wide as the screen
# @flag --maximized                     make the first frame maximized
# @flag -mm                             make the first frame maximized
# @option -g --geometry                 window geometry
# @flag --no-bitmap-icon                do not use picture of gnu for Emacs icon
# @flag -nbi                            do not use picture of gnu for Emacs icon
# @flag --iconic                        start Emacs in iconified state
# @option --internal-border <WIDTH>     width between text and main border
# @option -ib <WIDTH>                   width between text and main border
# @option --line-spacing <PIXELS>       additional space to put between lines
# @option -lsp <PIXELS>                 additional space to put between lines
# @option --mouse-color <COLOR>         mouse cursor color in Emacs window
# @option -ms <COLOR>                   mouse cursor color in Emacs window
# @option --name                        title for initial Emacs frame
# @flag --no-blinking-cursor            disable blinking cursor
# @flag -nbc                            disable blinking cursor
# @flag -r                              switch foreground and background
# @flag --reverse-video                 switch foreground and background
# @flag -rv                             switch foreground and background
# @option -T --title                    title for initial Emacs frame
# @flag --vertical-scroll-bars          enable vertical scroll bars
# @flag -vb                             enable vertical scroll bars
# @option --xrm <XRESOURCES>            set additional X resources
# @option --parent-id <XID>             set parent window
# @flag --help                          display this help and exit
# @flag --version                       output version information and exit

command eval "$(argc --argc-eval "$0" "$@")"