#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag --auto-tune                      sets all tunable options to their GOOD setting
# @flag -c --calibrate                   runs powertop in calibration mode
# @option -C --csv <filename>            generate a csv report
# @flag --debug                          run in "debug" mode
# @option --extech <devnode>             uses an Extech Power Analyzer for measurements
# @option -r --html <filename>           generate a html report
# @option -i --iteration <iterations>    number of times to run each test
# @flag -q --quiet                       suppress stderr output
# @option -s --sample <seconds>          interval for power consumption measurement
# @option -t --time <seconds>            generate a report for 'x' seconds
# @option -w --workload <workload>       file to execute for workload
# @flag -V --version                     print version information
# @flag -h --help                        print this help menu

command eval "$(argc --argc-eval "$0" "$@")"