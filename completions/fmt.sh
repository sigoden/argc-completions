#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -c --crown-margin         preserve indentation of first two lines
# @option -p --prefix <STRING>    reformat only lines beginning with STRING, reattaching the prefix to reformatted lines
# @flag -s --split-only           split long lines, but do not refill
# @flag -t --tagged-paragraph     indentation of first line different from second
# @flag -u --uniform-spacing      one space between words, two after sentences
# @option -w --width              maximum line width (default of 75 columns)
# @option -g --goal <WIDTH>       goal width (default of 93% of width)
# @flag --help                    display this help and exit
# @flag --version                 output version information and exit
# @arg file*

command eval "$(argc --argc-eval "$0" "$@")"