#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @option -c --bytes <[-]NUM>    print the first NUM bytes of each file; with the leading '-', print all but the last NUM bytes of each file
# @option -n --lines <[-]NUM>    print the first NUM lines instead of the first 10; with the leading '-', print all but the last NUM lines of each file
# @flag -q                       never print headers giving file names
# @flag --quiet                  never print headers giving file names
# @flag --silent                 never print headers giving file names
# @flag -v --verbose             always print headers giving file names
# @flag -z --zero-terminated     line delimiter is NUL, not newline
# @flag --help                   display this help and exit
# @flag --version                output version information and exit
# @arg file*

command eval "$(argc --argc-eval "$0" "$@")"