#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @option -o --output    Write the result to the file at path OUTPUT.
# @flag -y               Convert an identity file to a recipients file.

command eval "$(argc --argc-eval "$0" "$@")"