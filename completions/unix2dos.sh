#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag --allow-chown           allow file ownership change
# @flag -ascii                  convert only line breaks (default)
# @flag -iso                    conversion between DOS and ISO-8859-1 character set
# @flag -1252                   use Windows code page 1252 (Western European)
# @flag -437                    use DOS code page 437 (US) (default)
# @flag -850                    use DOS code page 850 (Western European)
# @flag -860                    use DOS code page 860 (Portuguese)
# @flag -863                    use DOS code page 863 (French Canadian)
# @flag -865                    use DOS code page 865 (Nordic)
# @flag -7                      convert 8 bit characters to 7 bit space
# @flag -b --keep-bom           keep Byte Order Mark (default)
# @flag -c --convmode           conversion mode
# @flag -e --add-eol            add a line break to the last line if there isn't one
# @flag -f --force              force conversion of binary files
# @flag -h --help               display this help text
# @option -i --info <FLAGS>     display file information
# @flag -k --keepdate           keep output file date
# @flag -L --license            display software license
# @flag -l --newline            add additional newline
# @flag -m --add-bom            add Byte Order Mark (default UTF-8)
# @flag -n --newfile            write to new file
# @flag --no-allow-chown        don't allow file ownership change (default)
# @flag --no-add-eol            don't add a line break to the last line if there isn't one (default)
# @flag -O --to-stdout          write to standard output
# @flag -o --oldfile            write to old file (default)
# @flag -q --quiet              quiet mode, suppress all warnings
# @flag -r --remove-bom         remove Byte Order Mark
# @flag -s --safe               skip binary files (default)
# @flag -u --keep-utf16         keep UTF-16 encoding
# @flag -ul                     assume that the input format is UTF-16LE
# @flag --assume-utf16le        assume that the input format is UTF-16LE
# @flag -ub                     assume that the input format is UTF-16BE
# @flag --assume-utf16be        assume that the input format is UTF-16BE
# @flag -v --verbose            verbose operation
# @flag -F --follow-symlink     follow symbolic links and convert the targets
# @flag -R --replace-symlink    replace symbolic links with converted files (original target files remain unchanged)
# @flag -S --skip-symlink       keep symbolic links and targets unchanged (default)
# @flag -V --version            display version number
# @arg file*

command eval "$(argc --argc-eval "$0" "$@")"