#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -a --active               active/inactive memory
# @flag -f --forks                number of forks since boot
# @flag -m --slabs                slabinfo
# @flag -n --one-header           do not redisplay header
# @flag -s --stats                event counter statistics
# @flag -d --disk                 disk statistics
# @flag -D --disk-sum             summarize disk statistics
# @option -p --partition <dev>    partition specific statistics
# @option -S --unit <char>        define display unit
# @flag -w --wide                 wide output
# @flag -t --timestamp            show timestamp
# @flag -h --help                 display this help and exit
# @flag -V --version              output version information and exit
# @arg delay
# @arg count

command eval "$(argc --argc-eval "$0" "$@")"