#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag --disable-history              Disables history when flag is present (default: false)
# @flag -h --help
# @flag -V --version
# @option -c --cpu-height <INT>        Min Percent Height of CPU/Memory visualization.
# @option --db <STRING>                Database to use, if any.
# @option -d --disk-height <INT>       Min Percent Height of Disk visualization.
# @option -n --net-height <INT>        Min Percent Height of Network visualization.
# @option -p --process-height <INT>    Min Percent Height of Process Table.
# @option -r --refresh-rate <INT>      Refresh rate in milliseconds.

command eval "$(argc --argc-eval "$0" "$@")"