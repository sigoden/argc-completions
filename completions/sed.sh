#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -n                            suppress automatic printing of pattern space
# @flag --quiet                       suppress automatic printing of pattern space
# @flag --silent                      suppress automatic printing of pattern space
# @flag --debug                       annotate program execution
# @option -e --expression <script>    add the script to the commands to be executed
# @option -f --file <script-file>     add the contents of script-file to the commands to be executed
# @flag --follow-symlinks             follow symlinks when processing in place
# @option -i --in-place <SUFFIX>      edit files in place (makes backup if SUFFIX supplied)
# @option -l --line-length <N>        specify the desired line-wrap length for the `l' command
# @flag --posix                       disable all GNU extensions.
# @flag -E                            use extended regular expressions in the script (for portability use POSIX -E).
# @flag -r                            use extended regular expressions in the script (for portability use POSIX -E).
# @flag --regexp-extended             use extended regular expressions in the script (for portability use POSIX -E).
# @flag -s --separate                 consider files as separate rather than as a single, continuous long stream.
# @flag --sandbox                     operate in sandbox mode (disable e/r/w commands).
# @flag -u --unbuffered               load minimal amounts of data from the input files and flush the output buffers more often
# @flag -z --null-data                separate lines by NUL characters
# @flag --help                        display this help and exit
# @flag --version                     output version information and exit
# @arg files*

command eval "$(argc --argc-eval "$0" "$@")"