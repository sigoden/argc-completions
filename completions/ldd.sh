#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag --help                  print this help and exit
# @flag --version               print version information and exit
# @flag -d --data-relocs        process data relocations
# @flag -r --function-relocs    process data and function relocations
# @flag -u --unused             print unused direct dependencies
# @flag -v --verbose            print all information
# @arg file*

command eval "$(argc --argc-eval "$0" "$@")"