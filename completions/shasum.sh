#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -a --algorithm      1 (default), 224, 256, 384, 512, 512224, 512256
# @flag -b --binary         read in binary mode
# @flag -c --check          read SHA sums from the FILEs and check them
# @flag --tag               create a BSD-style checksum
# @flag -t --text           read in text mode (default)
# @flag -U --UNIVERSAL      read in Universal Newlines mode produces same digest on Windows/Unix/Mac
# @flag -0 --01             read in BITS mode ASCII '0' interpreted as 0-bit, ASCII '1' interpreted as 1-bit, all other characters ignored
# @flag --ignore-missing    don't fail or report status for missing files
# @flag -q --quiet          don't print OK for each successfully verified file
# @flag -s --status         don't output anything, status code shows success
# @flag --strict            exit non-zero for improperly formatted checksum lines
# @flag -w --warn           warn about improperly formatted checksum lines
# @flag -h --help           display this help and exit
# @flag -v --version        output version information and exit
# @arg file*

command eval "$(argc --argc-eval "$0" "$@")"