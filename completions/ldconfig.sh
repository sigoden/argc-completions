#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @meta combine-shorts
# @option -c --format            Format to use: new (default), old, or compat
# @option -C <CACHE>             Use CACHE as cache file
# @option -f <CONF>              Use CONF as configuration file
# @flag -i --ignore-aux-cache    Ignore auxiliary cache file
# @flag -l                       Manually link individual libraries.
# @flag -n                       Only process directories specified on the command line.
# @flag -N                       Don't build cache
# @flag -p --print-cache         Print cache
# @option -r <ROOT>              Change to and use ROOT as root directory
# @flag -v --verbose             Generate verbose messages
# @flag -X                       Don't update symbolic links
# @flag -? --help                Give this help list
# @flag --usage                  Give a short usage message
# @flag -V --version             Print program version

command eval "$(argc --argc-eval "$0" "$@")"