#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -h --help                         show this help message and exit
# @option --config                        Use CONFIG instead of default config file.
# @flag -i --interactive                  Drop to bpython shell after running file instead of exiting.
# @flag -q --quiet                        Don't flush the output to stdout.
# @flag -V --version                      Print version and exit.
# @option -l --log-level[debug|info|warning|error|critical]  Set log level for logging
# @option -L --log-output <LOG_OUTPUT>    Log output file
# @flag -p --paste                        start by pasting lines of a file into session
# @arg file
# @arg args*

command eval "$(argc --argc-eval "$0" "$@")"