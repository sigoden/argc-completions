#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -h --help                    Show help options
# @option -c --config <FILE>         Use configuration file
# @flag -d --debug                   Print debugging messages
# @flag --test-mode                  Run as unprivileged user, skipping things that require root access
# @option --pid-file <FILE>          File to write PID into
# @option --log-dir <DIRECTORY>      Directory to write logs to
# @option --run-dir <DIRECTORY>      Directory to store running state
# @option --cache-dir <DIRECTORY>    Directory to cache information
# @flag --show-config                Show combined configuration
# @flag -v --version                 Show release version

command eval "$(argc --argc-eval "$0" "$@")"