#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -i --ignore-case              Ignore case distinctions
# @flag -P --pcre                     Use Perl compatible regular expressions (PCRE)
# @flag -H --with-filename            Print the file name for each match
# @flag -h --no-filename              Suppress the prefixing of file name on output
# @flag -n --page-number              Print page number with output lines
# @flag -c --count                    Print only a count of matches per file
# @option --color <WHEN>              Use colors for highlighting; WHEN can be `always', `never' or `auto'
# @flag -p --page-count               Print only a count of matches per page
# @option -m --max-count <NUM>        Stop reading after NUM matching lines (per file)
# @flag -q --quiet                    Suppress normal output
# @flag -r --recursive                Search directories recursively
# @flag -R --dereference-recursive    Likewise, but follow all symlinks
# @flag --cache                       Use cache for faster operation
# @flag --help                        Print this help
# @flag -V --version                  Show version information
# @arg pattern
# @arg file*

command eval "$(argc --argc-eval "$0" "$@")"