#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @option -v --volume                set the volume for playback of the file
# @flag -h --help                    print help
# @flag --leaks                      run leaks analysis
# @option -t --time                  play for TIME seconds
# @option -r --rate                  play at playback rate
# @option -q --rQuality <QUALITY>    set the quality used for rate-scaled playback (default is 0 - low quality, 1 - high quality)
# @flag -d --debug                   debug print output
# @arg audio_file

command eval "$(argc --argc-eval "$0" "$@")"