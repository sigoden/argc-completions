#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag --productName            Print only the value of the ProductName property.
# @flag --productVersion         Print only the value of the ProductVersion property.
# @flag --productVersionExtra    Print only the value of the ProductVersionExtra property.
# @flag --buildVersion           Print only the value of the BuildVersion property.

command eval "$(argc --argc-eval "$0" "$@")"