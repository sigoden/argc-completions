#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @option -attr <attr>     Fetches the value of the specified attribute
# @flag -count             Query only reports matching items count
# @option -onlyin <dir>    Search only within given directory
# @flag -live              Query should stay active
# @option -name <name>     Search on file name only
# @flag -reprint           Reprint results on live update
# @option -s <name>        Show contents of smart folder <name>
# @flag -0                 Use NUL (``\0'') as a path separator, for use with xargs -0.

command eval "$(argc --argc-eval "$0" "$@")"