#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -h --help               print this help message and exit
# @flag -p --print-path         print the path of the active developer directory
# @option -s --switch <path>    set the path for the active developer directory
# @flag --install               open a dialog for installation of the command line developer tools
# @flag -v --version            print the xcode-select version
# @flag -r --reset              reset to the default command line tools path

command eval "$(argc --argc-eval "$0" "$@")"