#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -d    Create an assertion to prevent the display from sleeping.
# @flag -i    Create an assertion to prevent the system from idle sleeping.
# @flag -m    Create an assertion to prevent the disk from idle sleeping.
# @flag -s    Create an assertion to prevent the system from sleeping.
# @flag -u    Create an assertion to declare that user is active.
# @flag -t    Specifies the timeout value in seconds for which this assertion has to be valid.
# @flag -w    Waits for the process with the specified pid to exit.

command eval "$(argc --argc-eval "$0" "$@")"