#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -i --inline            Inline the JSON output
# @option -q --query <FILE>    A query is sequence of tokens used to make a selection on a JSON input.
# @flag -r --raw-string        Write to stdout without JSON double-quotes (string only)
# @flag -s --stream            Read a stream of JSON data line by line
# @flag -v --validate          Validate the JSON data
# @flag -h --help              Print help (see a summary with '-h')
# @flag -V --version           Print version
# @arg query                   A query is sequence of tokens used to make a selection on a JSON input.
# @arg optional_file           JSON file to use

command eval "$(argc --argc-eval "$0" "$@")"