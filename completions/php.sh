#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -a                      Run as interactive shell (requires readline extension)
# @option -c <<path>|<file>>    Look for php.ini file in this directory
# @flag -n                      No configuration (ini) files will be used
# @option -d <foo[=bar]>        Define INI entry foo with value 'bar'
# @flag -e                      Generate extended information for debugger/profiler
# @option -f <file>             Parse and execute <file>.
# @flag -h                      This help
# @flag -i                      PHP information
# @flag -l                      Syntax check only (lint)
# @flag -m                      Show compiled in modules
# @option -r <code>             Run PHP <code> without using script tags <?..?>
# @option -B <begin_code>       Run PHP <begin_code> before processing input lines
# @option -R <code>             Run PHP <code> for every input line
# @option -F <file>             Parse and execute <file> for every input line
# @option -E <end_code>         Run PHP <end_code> after processing all input lines
# @flag -H                      Hide any passed arguments from external tools.
# @option -S <<addr>:<port>>    Run with built-in web server.
# @option -t <docroot>          Specify document root <docroot> for built-in web server.
# @flag -s                      Output HTML syntax highlighted source.
# @flag -v                      Version number
# @flag -w                      Output source with stripped comments and whitespace.
# @option -z <file>             Load Zend extension <file>.
# @flag --ini                   Show configuration file names
# @option --rf <name>           Show information about function <name>.
# @option --rc <name>           Show information about class <name>.
# @option --re <name>           Show information about extension <name>.
# @option --rz <name>           Show information about Zend extension <name>.
# @option --ri <name>           Show configuration for extension <name>.

command eval "$(argc --argc-eval "$0" "$@")"