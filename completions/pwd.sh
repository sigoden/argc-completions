#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -L    print the value of $PWD if it names the current working directory
# @flag -P    print the physical directory, without any symbolic links

command eval "$(argc --argc-eval "$0" "$@")"