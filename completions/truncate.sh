#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -c --no-create              do not create any files
# @flag -o --io-blocks              treat SIZE as number of IO blocks instead of bytes
# @option -r --reference <RFILE>    base size on RFILE
# @option -s --size                 set or adjust the file size by SIZE bytes
# @flag --help                      display this help and exit
# @flag --version                   output version information and exit
# @arg file*

command eval "$(argc --argc-eval "$0" "$@")"