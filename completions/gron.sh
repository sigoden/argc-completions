#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -u --ungron        Reverse the operation (turn assignments back into JSON)
# @flag -v --values        Print just the values of provided assignments
# @flag -c --colorize      Colorize output (default on tty)
# @flag -m --monochrome    Monochrome (don't colorize output)
# @flag -s --stream        Treat each line of input as a separate JSON object
# @flag -k --insecure      Disable certificate validation
# @flag -j --json          Represent gron data as JSON stream
# @flag --no-sort          Don't sort output (faster)
# @flag --version          Print version information
# @arg file-url <FILE|URL|->

command eval "$(argc --argc-eval "$0" "$@")"