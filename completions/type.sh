#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -a    display all locations containing an executable named NAME;
# @flag -f    suppress shell function lookup
# @flag -P    force a PATH search for each NAME, even if it is an alias,
# @flag -p    returns either the name of the disk file that would be executed,
# @flag -t    output a single word which is one of `alias', `keyword',

command eval "$(argc --argc-eval "$0" "$@")"