#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @option -e <stat>     execute string 'stat'
# @flag -i              enter interactive mode after executing 'script'
# @option -l <g=mod>    require library 'mod' into global 'g'
# @flag -v              show version information
# @flag -E              ignore environment variables
# @flag -W              turn warnings on
# @arg file-lua <file:.lua>
# @arg args*

command eval "$(argc --argc-eval "$0" "$@")"