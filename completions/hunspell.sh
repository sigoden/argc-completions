#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @meta combine-shorts
# @flag -1                    check only first field in lines (delimiter = tabulator)
# @flag -a                    Ispell's pipe interface
# @flag --check-url           check URLs, e-mail addresses and directory paths
# @flag --check-apostrophe    check Unicode typographic apostrophe
# @option -d <d[,d2,...]>     use d (d2 etc.) dictionaries
# @flag -D                    show available dictionaries
# @flag -G                    print only correct words or lines
# @flag -h --help             display this help and exit
# @flag -H                    HTML input file format
# @flag -i                    enc  input encoding
# @flag -l                    print misspelled words
# @flag -L                    print lines with misspelled words
# @flag -m                    analyze the words of the input text
# @flag -n                    nroff/troff input file format
# @flag -O                    OpenDocument (ODF or Flat ODF) input file format
# @flag -p                    dict  set dict custom dictionary
# @flag -r                    warn of the potential mistakes (rare words)
# @flag -P                    password  set password for encrypted dictionaries
# @flag -s                    stem the words of the input text
# @flag -S                    suffix words of the input text
# @flag -t                    TeX/LaTeX input file format
# @flag -v --version          print version number
# @flag -vv                   print Ispell compatible version number
# @flag -w                    print misspelled words (= lines) from one word/line input.
# @flag -X                    XML input file format
# @arg file*

command eval "$(argc --argc-eval "$0" "$@")"