#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -c --check        Returns non-zero exit code if Cargo.toml is unsorted, overrides default behavior
# @flag -p --print        Prints Cargo.toml, lexically sorted, to stdout
# @flag -n --no-format    Skips formatting after sorting
# @flag --check-format    Also returns non-zero exit code if formatting changes
# @flag -w --workspace    Checks every crate in a workspace
# @flag -g --grouped      Keep blank lines when sorting groups of key value pairs
# @option -o --order[package|dependencies|feature] <order>  List the order tables should be written out (--order package,dependencies,features)
# @flag -h --help         Print help information
# @flag -V --version      Print version information
# @arg paths*

command eval "$(argc --argc-eval "$0" "$@")"