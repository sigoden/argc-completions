#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @meta combine-shorts
# @flag -1    VT100 mode.
# @flag -b    Binary mode.
# @flag -C    Disable color support.
# @flag -c    Make searches case-sensitive
# @flag -d    Omit the backslash mark used to denote a wrapped line.
# @flag -M    Disable the use of mmap.
# @flag -r    Default to using regexp searches
# @flag -s    Squeeze-mode.
# @flag -t    Display tabs as ^I.
# @flag -u    Disable UTF-8 mode even if the locale dictates it
# @flag -v    Display control characters as in `^A' for control A.
# @flag -w    Wrap lines
# @flag -z    Disable gunzip-on-the-fly
# @arg filename*

command eval "$(argc --argc-eval "$0" "$@")"