#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -S    use the `soft' resource limit
# @flag -H    use the `hard' resource limit
# @flag -a    all current limits are reported
# @flag -b    the socket buffer size
# @flag -c    the maximum size of core files created
# @flag -d    the maximum size of a process's data segment
# @flag -e    the maximum scheduling priority (`nice')
# @flag -f    the maximum size of files written by the shell and its children
# @flag -i    the maximum number of pending signals
# @flag -k    the maximum number of kqueues allocated for this process
# @flag -l    the maximum size a process may lock into memory
# @flag -m    the maximum resident set size
# @flag -n    the maximum number of open file descriptors
# @flag -p    the pipe buffer size
# @flag -q    the maximum number of bytes in POSIX message queues
# @flag -r    the maximum real-time scheduling priority
# @flag -s    the maximum stack size
# @flag -t    the maximum amount of cpu time in seconds
# @flag -u    the maximum number of user processes
# @flag -v    the size of virtual memory
# @flag -x    the maximum number of file locks
# @flag -P    the maximum number of pseudoterminals
# @flag -R    the maximum time a real-time process can run before blocking
# @flag -T    the maximum number of threads

command eval "$(argc --argc-eval "$0" "$@")"