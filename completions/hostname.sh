#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -a --alias               alias names
# @flag -A --all-fqdns           all long host names (FQDNs)
# @flag -b --boot                set default hostname if none available
# @flag -d --domain              DNS domain name
# @flag -f                       long host name (FQDN)
# @flag --fqdn                   long host name (FQDN)
# @flag --long                   long host name (FQDN)
# @option -F --file              read host name or NIS domain name from given file
# @flag -i --ip-address          addresses for the host name
# @flag -I --all-ip-addresses    all addresses for the host
# @flag -s --short               short host name
# @flag -y                       NIS/YP domain name
# @flag --yp                     NIS/YP domain name
# @flag --nis                    NIS/YP domain name

command eval "$(argc --argc-eval "$0" "$@")"