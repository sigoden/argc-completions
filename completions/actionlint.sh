#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -color                      Always enable colorful output.
# @option -config-file <file>       File path to config file
# @flag -debug                      Enable debug output (for development)
# @option -format <string>          Custom template to format error messages in Go template syntax.
# @option -ignore <value>           Regular expression matching to error messages you want to ignore.
# @flag -init-config                Generate default config file at .github/actionlint.yaml in current project
# @flag -no-color                   Disable colorful output
# @flag -oneline                    Use one line per one error.
# @option -pyflakes <file>          Command name or file path of "pyflakes" external command.
# @option -shellcheck <file>        Command name or file path of "shellcheck" external command.
# @option -stdin-filename <file>    File name when reading input from stdin
# @flag -verbose                    Enable verbose output
# @flag -version                    Show version and how this binary was installed
# @arg files*

command eval "$(argc --argc-eval "$0" "$@")"