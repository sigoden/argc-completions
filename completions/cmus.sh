#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @option --listen <ADDR>    listen on ADDR instead of $CMUS_SOCKET or $XDG_RUNTIME_DIR/cmus-socket ADDR is either a UNIX socket or host[:port] WARNING: using TCP/IP is insecure!
# @flag --plugins            list available plugins and exit
# @flag --show-cursor        always visible cursor
# @flag --help               display this help and exit
# @flag --version            v2.10.0

command eval "$(argc --argc-eval "$0" "$@")"