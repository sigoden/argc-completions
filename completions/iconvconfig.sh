#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag --nostdlib              Do not search standard directories, only those on the command line
# @option -o --output <FILE>    Put output in FILE instead of installed location (--prefix does not apply to FILE)
# @option --prefix <PATH>       Prefix used for all file accesses
# @flag -? --help               Give this help list
# @flag --usage                 Give a short usage message
# @flag -V --version            Print program version
# @arg dir*

command eval "$(argc --argc-eval "$0" "$@")"