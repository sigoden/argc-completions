#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @meta combine-shorts
# @flag -E --error-on-no-fonts    raise an error if no fonts in a directory
# @flag -f --force                scan directories with apparently valid caches
# @flag -r --really-force         erase all existing caches, then rescan
# @flag -s --system-only          scan system-wide directories only
# @option -y --sysroot            prepend SYSROOT to all paths for scanning
# @flag -v --verbose              display status information while busy
# @flag -V --version              display font config version and exit
# @flag -h --help                 display this help and exit
# @arg dirs*

command eval "$(argc --argc-eval "$0" "$@")"