#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -h --help                     Show help options
# @flag --help-all                    Show all help options
# @flag --help-sm-client              Show session management options
# @flag --help-gtk                    Show GTK+ Options
# @option -p --page-label <PAGE>      The page label of the document to display.
# @option -i --page-index <NUMBER>    The page number of the document to display.
# @flag -f --fullscreen               Run xreader in fullscreen mode
# @option -n --named-dest <DEST>      Named destination to display.
# @flag -s --presentation             Run xreader in presentation mode
# @flag -w --preview                  Run xreader as a previewer
# @option -l --find <STRING>          The word or phrase to find in the document
# @option --display                   X display to use
# @arg file*

command eval "$(argc --argc-eval "$0" "$@")"