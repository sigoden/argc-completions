#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag --help            Show this help
# @flag --halt            Halt the machine
# @flag -p --poweroff     Switch off the machine
# @flag --reboot          Reboot the machine
# @flag -f --force        Force immediate halt/power-off/reboot
# @flag -w --wtmp-only    Don't halt/power-off/reboot, just write wtmp record
# @flag -d --no-wtmp      Don't write wtmp record
# @flag --no-wall         Don't send wall message before halt/power-off/reboot

command eval "$(argc --argc-eval "$0" "$@")"