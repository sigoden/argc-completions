#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @option --burst-limit <int>                client-side default throttling limit (default 100)
# @flag --debug                              enable verbose output
# @flag -h --help                            help for helm
# @option --kube-apiserver <string>          the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>          group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>            username to impersonate for the operation
# @option --kube-ca-file <file>              the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify      if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>    server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>              bearer token used for authentication
# @option --kubeconfig <file>                path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>           path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>          path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>         path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")

# {{ helm completion
# @cmd generate autocompletion scripts for the specified shell
# @flag -h --help                            help for completion
# @option --burst-limit <int>                client-side default throttling limit (default 100)
# @flag --debug                              enable verbose output
# @option --kube-apiserver <string>          the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>          group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>            username to impersonate for the operation
# @option --kube-ca-file <file>              the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify      if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>    server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>              bearer token used for authentication
# @option --kubeconfig <file>                path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>           path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>          path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>         path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
completion() {
    :;
}

# {{{ helm completion bash
# @cmd generate autocompletion script for bash
# @flag -h --help                            help for bash
# @flag --no-descriptions                    disable completion descriptions
# @option --burst-limit <int>                client-side default throttling limit (default 100)
# @flag --debug                              enable verbose output
# @option --kube-apiserver <string>          the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>          group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>            username to impersonate for the operation
# @option --kube-ca-file <file>              the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify      if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>    server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>              bearer token used for authentication
# @option --kubeconfig <file>                path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>           path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>          path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>         path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
completion::bash() {
    :;
}
# }}} helm completion bash

# {{{ helm completion fish
# @cmd generate autocompletion script for fish
# @flag -h --help                            help for fish
# @flag --no-descriptions                    disable completion descriptions
# @option --burst-limit <int>                client-side default throttling limit (default 100)
# @flag --debug                              enable verbose output
# @option --kube-apiserver <string>          the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>          group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>            username to impersonate for the operation
# @option --kube-ca-file <file>              the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify      if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>    server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>              bearer token used for authentication
# @option --kubeconfig <file>                path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>           path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>          path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>         path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
completion::fish() {
    :;
}
# }}} helm completion fish

# {{{ helm completion powershell
# @cmd generate autocompletion script for powershell
# @flag -h --help                            help for powershell
# @flag --no-descriptions                    disable completion descriptions
# @option --burst-limit <int>                client-side default throttling limit (default 100)
# @flag --debug                              enable verbose output
# @option --kube-apiserver <string>          the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>          group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>            username to impersonate for the operation
# @option --kube-ca-file <file>              the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify      if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>    server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>              bearer token used for authentication
# @option --kubeconfig <file>                path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>           path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>          path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>         path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
completion::powershell() {
    :;
}
# }}} helm completion powershell

# {{{ helm completion zsh
# @cmd generate autocompletion script for zsh
# @flag -h --help                            help for zsh
# @flag --no-descriptions                    disable completion descriptions
# @option --burst-limit <int>                client-side default throttling limit (default 100)
# @flag --debug                              enable verbose output
# @option --kube-apiserver <string>          the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>          group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>            username to impersonate for the operation
# @option --kube-ca-file <file>              the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify      if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>    server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>              bearer token used for authentication
# @option --kubeconfig <file>                path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>           path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>          path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>         path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
completion::zsh() {
    :;
}
# }}} helm completion zsh
# }} helm completion

# {{ helm create
# @cmd create a new chart with the given name
# @flag -h --help                            help for create
# @option -p --starter <path>                the name or absolute path to Helm starter scaffold
# @option --burst-limit <int>                client-side default throttling limit (default 100)
# @flag --debug                              enable verbose output
# @option --kube-apiserver <string>          the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>          group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>            username to impersonate for the operation
# @option --kube-ca-file <file>              the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify      if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>    server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>              bearer token used for authentication
# @option --kubeconfig <file>                path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>           path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>          path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>         path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
# @arg name
create() {
    :;
}
# }} helm create

# {{ helm dependency
# @cmd manage a chart's dependencies
# @alias dep,dependencies
# @flag -h --help                            help for dependency
# @option --burst-limit <int>                client-side default throttling limit (default 100)
# @flag --debug                              enable verbose output
# @option --kube-apiserver <string>          the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>          group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>            username to impersonate for the operation
# @option --kube-ca-file <file>              the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify      if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>    server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>              bearer token used for authentication
# @option --kubeconfig <file>                path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>           path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>          path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>         path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
dependency() {
    :;
}

# {{{ helm dependency build
# @cmd rebuild the charts/ directory based on the Chart.lock file
# @flag -h --help                            help for build
# @option --keyring <file>                   keyring containing public keys (default "/home/sigo/.gnupg/pubring.gpg")
# @flag --skip-refresh                       do not refresh the local repository cache
# @flag --verify                             verify the packages against signatures
# @option --burst-limit <int>                client-side default throttling limit (default 100)
# @flag --debug                              enable verbose output
# @option --kube-apiserver <string>          the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>          group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>            username to impersonate for the operation
# @option --kube-ca-file <file>              the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify      if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>    server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>              bearer token used for authentication
# @option --kubeconfig <file>                path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>           path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>          path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>         path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
# @arg chart[`_choice_chart`]
dependency::build() {
    :;
}
# }}} helm dependency build

# {{{ helm dependency list
# @cmd list the dependencies for the given chart
# @flag -h --help                            help for list
# @option --max-col-width <uint>             maximum column width for output table (default 80)
# @option --burst-limit <int>                client-side default throttling limit (default 100)
# @flag --debug                              enable verbose output
# @option --kube-apiserver <string>          the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>          group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>            username to impersonate for the operation
# @option --kube-ca-file <file>              the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify      if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>    server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>              bearer token used for authentication
# @option --kubeconfig <file>                path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>           path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>          path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>         path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
# @arg chart[`_choice_chart`]
dependency::list() {
    :;
}
# }}} helm dependency list

# {{{ helm dependency update
# @cmd update charts/ based on the contents of Chart.yaml
# @flag -h --help                            help for update
# @option --keyring <file>                   keyring containing public keys (default "/home/sigo/.gnupg/pubring.gpg")
# @flag --skip-refresh                       do not refresh the local repository cache
# @flag --verify                             verify the packages against signatures
# @option --burst-limit <int>                client-side default throttling limit (default 100)
# @flag --debug                              enable verbose output
# @option --kube-apiserver <string>          the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>          group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>            username to impersonate for the operation
# @option --kube-ca-file <file>              the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify      if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>    server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>              bearer token used for authentication
# @option --kubeconfig <file>                path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>           path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>          path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>         path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
# @arg chart[`_choice_chart`]
dependency::update() {
    :;
}
# }}} helm dependency update
# }} helm dependency

# {{ helm env
# @cmd helm client environment information
# @flag -h --help                            help for env
# @option --burst-limit <int>                client-side default throttling limit (default 100)
# @flag --debug                              enable verbose output
# @option --kube-apiserver <string>          the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>          group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>            username to impersonate for the operation
# @option --kube-ca-file <file>              the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify      if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>    server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>              bearer token used for authentication
# @option --kubeconfig <file>                path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>           path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>          path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>         path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
env() {
    :;
}
# }} helm env

# {{ helm get
# @cmd download extended information of a named release
# @flag -h --help                            help for get
# @option --burst-limit <int>                client-side default throttling limit (default 100)
# @flag --debug                              enable verbose output
# @option --kube-apiserver <string>          the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>          group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>            username to impersonate for the operation
# @option --kube-ca-file <file>              the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify      if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>    server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>              bearer token used for authentication
# @option --kubeconfig <file>                path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>           path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>          path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>         path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
get() {
    :;
}

# {{{ helm get all
# @cmd download all information for a named release
# @flag -h --help                            help for all
# @option --revision <int>                   get the named release with revision
# @option --template <string>                go template for formatting the output, eg: {{.Release.Name}}
# @option --burst-limit <int>                client-side default throttling limit (default 100)
# @flag --debug                              enable verbose output
# @option --kube-apiserver <string>          the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>          group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>            username to impersonate for the operation
# @option --kube-ca-file <file>              the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify      if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>    server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>              bearer token used for authentication
# @option --kubeconfig <file>                path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>           path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>          path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>         path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
# @arg release_name[`_choice_release`]
get::all() {
    :;
}
# }}} helm get all

# {{{ helm get hooks
# @cmd download all hooks for a named release
# @flag -h --help                            help for hooks
# @option --revision <int>                   get the named release with revision
# @option --burst-limit <int>                client-side default throttling limit (default 100)
# @flag --debug                              enable verbose output
# @option --kube-apiserver <string>          the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>          group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>            username to impersonate for the operation
# @option --kube-ca-file <file>              the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify      if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>    server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>              bearer token used for authentication
# @option --kubeconfig <file>                path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>           path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>          path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>         path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
# @arg release_name[`_choice_release`]
get::hooks() {
    :;
}
# }}} helm get hooks

# {{{ helm get manifest
# @cmd download the manifest for a named release
# @flag -h --help                            help for manifest
# @option --revision <int>                   get the named release with revision
# @option --burst-limit <int>                client-side default throttling limit (default 100)
# @flag --debug                              enable verbose output
# @option --kube-apiserver <string>          the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>          group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>            username to impersonate for the operation
# @option --kube-ca-file <file>              the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify      if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>    server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>              bearer token used for authentication
# @option --kubeconfig <file>                path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>           path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>          path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>         path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
# @arg release_name[`_choice_release`]
get::manifest() {
    :;
}
# }}} helm get manifest

# {{{ helm get metadata
# @cmd This command fetches metadata for a given release
# @flag -h --help                                  help for metadata
# @option -o --output[table|json|yaml] <format>    prints the output in the specified format.
# @option --revision <int>                         specify release revision
# @option --burst-limit <int>                      client-side default throttling limit (default 100)
# @flag --debug                                    enable verbose output
# @option --kube-apiserver <string>                the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>                group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>                  username to impersonate for the operation
# @option --kube-ca-file <file>                    the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify            if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>          server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>                    bearer token used for authentication
# @option --kubeconfig <file>                      path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>                 path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>                path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>               path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
# @arg release_name[`_choice_release`]
get::metadata() {
    :;
}
# }}} helm get metadata

# {{{ helm get notes
# @cmd download the notes for a named release
# @flag -h --help                            help for notes
# @option --revision <int>                   get the named release with revision
# @option --burst-limit <int>                client-side default throttling limit (default 100)
# @flag --debug                              enable verbose output
# @option --kube-apiserver <string>          the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>          group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>            username to impersonate for the operation
# @option --kube-ca-file <file>              the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify      if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>    server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>              bearer token used for authentication
# @option --kubeconfig <file>                path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>           path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>          path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>         path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
# @arg release_name[`_choice_release`]
get::notes() {
    :;
}
# }}} helm get notes

# {{{ helm get values
# @cmd download the values file for a named release
# @flag -a --all                                   dump all (computed) values
# @flag -h --help                                  help for values
# @option -o --output[table|json|yaml] <format>    prints the output in the specified format.
# @option --revision <int>                         get the named release with revision
# @option --burst-limit <int>                      client-side default throttling limit (default 100)
# @flag --debug                                    enable verbose output
# @option --kube-apiserver <string>                the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>                group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>                  username to impersonate for the operation
# @option --kube-ca-file <file>                    the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify            if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>          server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>                    bearer token used for authentication
# @option --kubeconfig <file>                      path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>                 path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>                path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>               path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
# @arg release_name[`_choice_release`]
get::values() {
    :;
}
# }}} helm get values
# }} helm get

# {{ helm history
# @cmd fetch release history
# @alias hist
# @flag -h --help                                  help for history
# @option --max <int>                              maximum number of revision to include in history (default 256)
# @option -o --output[table|json|yaml] <format>    prints the output in the specified format.
# @option --burst-limit <int>                      client-side default throttling limit (default 100)
# @flag --debug                                    enable verbose output
# @option --kube-apiserver <string>                the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>                group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>                  username to impersonate for the operation
# @option --kube-ca-file <file>                    the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify            if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>          server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>                    bearer token used for authentication
# @option --kubeconfig <file>                      path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>                 path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>                path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>               path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
# @arg release_name[`_choice_release`]
history() {
    :;
}
# }} helm history

# {{ helm install
# @cmd install a chart
# @flag --atomic                                   if set, the installation process deletes the installation on failure.
# @option --ca-file <file>                         verify certificates of HTTPS-enabled servers using this CA bundle
# @option --cert-file <file>                       identify HTTPS client using this SSL certificate file
# @flag --create-namespace                         create the release namespace if not present
# @flag --dependency-update                        update dependencies if they are missing before installing the chart
# @option --description <string>                   add a custom description
# @flag --devel                                    use development versions, too.
# @flag --disable-openapi-validation               if set, the installation process will not validate rendered templates against the Kubernetes OpenAPI Schema
# @option --dry-run <string[="client"]>            simulate an install.
# @flag --enable-dns                               enable DNS lookups when rendering templates
# @flag --force                                    force resource updates through a replacement strategy
# @flag -g --generate-name                         generate the name (and omit the NAME parameter)
# @flag -h --help                                  help for install
# @flag --insecure-skip-tls-verify                 skip tls certificate checks for the chart download
# @option --key-file <file>                        identify HTTPS client using this SSL key file
# @option --keyring <file>                         location of public keys used for verification (default "/home/sigo/.gnupg/pubring.gpg")
# @option -l --labels <stringToString>             Labels that would be added to release metadata.
# @option --name-template <string>                 specify template used to name the release
# @flag --no-hooks                                 prevent hooks from running during install
# @option -o --output[table|json|yaml] <format>    prints the output in the specified format.
# @flag --pass-credentials                         pass credentials to all domains
# @option --password <string>                      chart repository password where to locate the requested chart
# @flag --plain-http                               use insecure HTTP connections for the chart download
# @option --post-renderer <path>                   the path to an executable to be used for post rendering.
# @option --post-renderer-args <postRendererArgsSlice>  an argument to the post-renderer (can specify multiple) (default [])
# @flag --render-subchart-notes                    if set, render subchart notes along with the parent
# @flag --replace                                  re-use the given name, only if that name is a deleted release which remains in the history.
# @option --repo <string>                          chart repository url where to locate the requested chart
# @option --set* <string>                          set values on the command line (can specify multiple or separate values with commas: key1=val1,key2=val2)
# @option --set-file*,[`_choice_set_file`] <file>  set values from respective files specified via the command line (can specify multiple or separate values with commas: key1=path1,key2=path2)
# @option --set-json* <string>                     set JSON values on the command line (can specify multiple or separate values with commas: key1=jsonval1,key2=jsonval2)
# @option --set-literal* <string>                  set a literal STRING value on the command line
# @option --set-string* <string>                   set STRING values on the command line (can specify multiple or separate values with commas: key1=val1,key2=val2)
# @flag --skip-crds                                if set, no CRDs will be installed.
# @option --timeout <duration>                     time to wait for any individual Kubernetes operation (like Jobs for hooks) (default 5m0s)
# @option --username <string>                      chart repository username where to locate the requested chart
# @option -f --values* <file>                      specify values in a YAML file or a URL (can specify multiple)
# @flag --verify                                   verify the package before using it
# @option --version <string>                       specify a version constraint for the chart version to use.
# @flag --wait                                     if set, will wait until all Pods, PVCs, Services, and minimum number of Pods of a Deployment, StatefulSet, or ReplicaSet are in a ready state before marking the release as successful.
# @flag --wait-for-jobs                            if set and --wait enabled, will wait until all Jobs have been completed before marking the release as successful.
# @option --burst-limit <int>                      client-side default throttling limit (default 100)
# @flag --debug                                    enable verbose output
# @option --kube-apiserver <string>                the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>                group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>                  username to impersonate for the operation
# @option --kube-ca-file <file>                    the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify            if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>          server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>                    bearer token used for authentication
# @option --kubeconfig <file>                      path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>                 path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>                path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>               path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
# @arg name
# @arg chart[`_choice_chart`]
install() {
    :;
}
# }} helm install

# {{ helm lint
# @cmd examine a chart for possible issues
# @flag -h --help                            help for lint
# @flag --quiet                              print only warnings and errors
# @option --set* <string>                    set values on the command line (can specify multiple or separate values with commas: key1=val1,key2=val2)
# @option --set-file*,[`_choice_set_file`] <file>  set values from respective files specified via the command line (can specify multiple or separate values with commas: key1=path1,key2=path2)
# @option --set-json* <string>               set JSON values on the command line (can specify multiple or separate values with commas: key1=jsonval1,key2=jsonval2)
# @option --set-literal* <string>            set a literal STRING value on the command line
# @option --set-string* <string>             set STRING values on the command line (can specify multiple or separate values with commas: key1=val1,key2=val2)
# @flag --strict                             fail on lint warnings
# @option -f --values* <file>                specify values in a YAML file or a URL (can specify multiple)
# @flag --with-subcharts                     lint dependent charts
# @option --burst-limit <int>                client-side default throttling limit (default 100)
# @flag --debug                              enable verbose output
# @option --kube-apiserver <string>          the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>          group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>            username to impersonate for the operation
# @option --kube-ca-file <file>              the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify      if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>    server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>              bearer token used for authentication
# @option --kubeconfig <file>                path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>           path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>          path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>         path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
# @arg path
lint() {
    :;
}
# }} helm lint

# {{ helm list
# @cmd list releases
# @alias ls
# @flag -a --all                                   show all releases without any filter applied
# @flag -A --all-namespaces                        list releases across all namespaces
# @flag -d --date                                  sort by release date
# @flag --deployed                                 show deployed releases.
# @flag --failed                                   show failed releases
# @option -f --filter <string>                     a regular expression (Perl compatible).
# @flag -h --help                                  help for list
# @option -m --max <int>                           maximum number of releases to fetch (default 256)
# @flag --no-headers                               don't print headers when using the default output format
# @option --offset <int>                           next release index in the list, used to offset from start value
# @option -o --output[table|json|yaml] <format>    prints the output in the specified format.
# @flag --pending                                  show pending releases
# @flag -r --reverse                               reverse the sort order
# @option -l --selector <string>                   Selector (label query) to filter on, supports '=', '==', and '!='.(e.g. -l key1=value1,key2=value2).
# @flag -q --short                                 output short (quiet) listing format
# @flag --superseded                               show superseded releases
# @option --time-format <string>                   format time using golang time formatter.
# @flag --uninstalled                              show uninstalled releases (if 'helm uninstall --keep-history' was used)
# @flag --uninstalling                             show releases that are currently being uninstalled
# @option --burst-limit <int>                      client-side default throttling limit (default 100)
# @flag --debug                                    enable verbose output
# @option --kube-apiserver <string>                the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>                group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>                  username to impersonate for the operation
# @option --kube-ca-file <file>                    the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify            if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>          server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>                    bearer token used for authentication
# @option --kubeconfig <file>                      path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>                 path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>                path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>               path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
list() {
    :;
}
# }} helm list

# {{ helm package
# @cmd package a chart directory into a chart archive
# @option --app-version <string>             set the appVersion on the chart to this version
# @flag -u --dependency-update               update dependencies from "Chart.yaml" to dir "charts/" before packaging
# @option -d --destination <path>            location to write the chart.
# @flag -h --help                            help for package
# @option --key <string>                     name of the key to use when signing.
# @option --keyring <file>                   location of a public keyring (default "/home/sigo/.gnupg/pubring.gpg")
# @option --passphrase-file <file>           location of a file which contains the passphrase for the signing key.
# @flag --sign                               use a PGP private key to sign this package
# @option --version <string>                 set the version on the chart to this semver version
# @option --burst-limit <int>                client-side default throttling limit (default 100)
# @flag --debug                              enable verbose output
# @option --kube-apiserver <string>          the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>          group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>            username to impersonate for the operation
# @option --kube-ca-file <file>              the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify      if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>    server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>              bearer token used for authentication
# @option --kubeconfig <file>                path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>           path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>          path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>         path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
# @arg chart_path
package() {
    :;
}
# }} helm package

# {{ helm plugin
# @cmd install, list, or uninstall Helm plugins
# @flag -h --help                            help for plugin
# @option --burst-limit <int>                client-side default throttling limit (default 100)
# @flag --debug                              enable verbose output
# @option --kube-apiserver <string>          the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>          group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>            username to impersonate for the operation
# @option --kube-ca-file <file>              the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify      if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>    server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>              bearer token used for authentication
# @option --kubeconfig <file>                path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>           path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>          path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>         path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
plugin() {
    :;
}

# {{{ helm plugin install
# @cmd install one or more Helm plugins
# @alias add
# @flag -h --help                            help for install
# @option --version <string>                 specify a version constraint.
# @option --burst-limit <int>                client-side default throttling limit (default 100)
# @flag --debug                              enable verbose output
# @option --kube-apiserver <string>          the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>          group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>            username to impersonate for the operation
# @option --kube-ca-file <file>              the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify      if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>    server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>              bearer token used for authentication
# @option --kubeconfig <file>                path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>           path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>          path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>         path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
# @arg path-url+ <path|url>
plugin::install() {
    :;
}
# }}} helm plugin install

# {{{ helm plugin list
# @cmd list installed Helm plugins
# @alias ls
# @flag -h --help                            help for list
# @option --burst-limit <int>                client-side default throttling limit (default 100)
# @flag --debug                              enable verbose output
# @option --kube-apiserver <string>          the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>          group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>            username to impersonate for the operation
# @option --kube-ca-file <file>              the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify      if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>    server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>              bearer token used for authentication
# @option --kubeconfig <file>                path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>           path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>          path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>         path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
plugin::list() {
    :;
}
# }}} helm plugin list

# {{{ helm plugin uninstall
# @cmd uninstall one or more Helm plugins
# @alias rm,remove
# @flag -h --help                            help for uninstall
# @option --burst-limit <int>                client-side default throttling limit (default 100)
# @flag --debug                              enable verbose output
# @option --kube-apiserver <string>          the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>          group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>            username to impersonate for the operation
# @option --kube-ca-file <file>              the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify      if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>    server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>              bearer token used for authentication
# @option --kubeconfig <file>                path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>           path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>          path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>         path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
# @arg plugin+[`_choice_plugin`]
plugin::uninstall() {
    :;
}
# }}} helm plugin uninstall

# {{{ helm plugin update
# @cmd update one or more Helm plugins
# @alias up
# @flag -h --help                            help for update
# @option --burst-limit <int>                client-side default throttling limit (default 100)
# @flag --debug                              enable verbose output
# @option --kube-apiserver <string>          the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>          group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>            username to impersonate for the operation
# @option --kube-ca-file <file>              the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify      if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>    server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>              bearer token used for authentication
# @option --kubeconfig <file>                path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>           path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>          path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>         path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
# @arg plugin+[`_choice_plugin`]
plugin::update() {
    :;
}
# }}} helm plugin update
# }} helm plugin

# {{ helm pull
# @cmd download a chart from a repository and (optionally) unpack it in local directory
# @alias fetch
# @option --ca-file <file>                   verify certificates of HTTPS-enabled servers using this CA bundle
# @option --cert-file <file>                 identify HTTPS client using this SSL certificate file
# @option -d --destination <path>            location to write the chart.
# @flag --devel                              use development versions, too.
# @flag -h --help                            help for pull
# @flag --insecure-skip-tls-verify           skip tls certificate checks for the chart download
# @option --key-file <file>                  identify HTTPS client using this SSL key file
# @option --keyring <file>                   location of public keys used for verification (default "/home/sigo/.gnupg/pubring.gpg")
# @flag --pass-credentials                   pass credentials to all domains
# @option --password <string>                chart repository password where to locate the requested chart
# @flag --plain-http                         use insecure HTTP connections for the chart download
# @flag --prov                               fetch the provenance file, but don't perform verification
# @option --repo <string>                    chart repository url where to locate the requested chart
# @flag --untar                              if set to true, will untar the chart after downloading it
# @option --untardir <dir>                   if untar is specified, this flag specifies the name of the directory into which the chart is expanded (default ".")
# @option --username <string>                chart repository username where to locate the requested chart
# @flag --verify                             verify the package before using it
# @option --version <string>                 specify a version constraint for the chart version to use.
# @option --burst-limit <int>                client-side default throttling limit (default 100)
# @flag --debug                              enable verbose output
# @option --kube-apiserver <string>          the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>          group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>            username to impersonate for the operation
# @option --kube-ca-file <file>              the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify      if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>    server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>              bearer token used for authentication
# @option --kubeconfig <file>                path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>           path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>          path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>         path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
# @arg chart[`_choice_repo_chart`]
pull() {
    :;
}
# }} helm pull

# {{ helm push
# @cmd push a chart to remote
# @option --ca-file <file>                   verify certificates of HTTPS-enabled servers using this CA bundle
# @option --cert-file <file>                 identify registry client using this SSL certificate file
# @flag -h --help                            help for push
# @flag --insecure-skip-tls-verify           skip tls certificate checks for the chart upload
# @option --key-file <file>                  identify registry client using this SSL key file
# @flag --plain-http                         use insecure HTTP connections for the chart upload
# @option --burst-limit <int>                client-side default throttling limit (default 100)
# @flag --debug                              enable verbose output
# @option --kube-apiserver <string>          the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>          group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>            username to impersonate for the operation
# @option --kube-ca-file <file>              the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify      if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>    server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>              bearer token used for authentication
# @option --kubeconfig <file>                path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>           path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>          path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>         path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
# @arg dir![`_choice_chart`]
# @arg remote
push() {
    :;
}
# }} helm push

# {{ helm registry
# @cmd login to or logout from a registry
# @flag -h --help                            help for registry
# @option --burst-limit <int>                client-side default throttling limit (default 100)
# @flag --debug                              enable verbose output
# @option --kube-apiserver <string>          the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>          group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>            username to impersonate for the operation
# @option --kube-ca-file <file>              the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify      if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>    server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>              bearer token used for authentication
# @option --kubeconfig <file>                path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>           path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>          path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>         path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
registry() {
    :;
}

# {{{ helm registry login
# @cmd login to a registry
# @option --ca-file <file>                   verify certificates of HTTPS-enabled servers using this CA bundle
# @option --cert-file <file>                 identify registry client using this SSL certificate file
# @flag -h --help                            help for login
# @flag --insecure                           allow connections to TLS registry without certs
# @option --key-file <file>                  identify registry client using this SSL key file
# @option -p --password <string>             registry password or identity token
# @flag --password-stdin                     read password or identity token from stdin
# @option -u --username <string>             registry username
# @option --burst-limit <int>                client-side default throttling limit (default 100)
# @flag --debug                              enable verbose output
# @option --kube-apiserver <string>          the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>          group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>            username to impersonate for the operation
# @option --kube-ca-file <file>              the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify      if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>    server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>              bearer token used for authentication
# @option --kubeconfig <file>                path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>           path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>          path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>         path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
# @arg host
registry::login() {
    :;
}
# }}} helm registry login

# {{{ helm registry logout
# @cmd logout from a registry
# @flag -h --help                            help for logout
# @option --burst-limit <int>                client-side default throttling limit (default 100)
# @flag --debug                              enable verbose output
# @option --kube-apiserver <string>          the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>          group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>            username to impersonate for the operation
# @option --kube-ca-file <file>              the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify      if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>    server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>              bearer token used for authentication
# @option --kubeconfig <file>                path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>           path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>          path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>         path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
# @arg host
registry::logout() {
    :;
}
# }}} helm registry logout
# }} helm registry

# {{ helm repo
# @cmd add, list, remove, update, and index chart repositories
# @flag -h --help                            help for repo
# @option --burst-limit <int>                client-side default throttling limit (default 100)
# @flag --debug                              enable verbose output
# @option --kube-apiserver <string>          the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>          group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>            username to impersonate for the operation
# @option --kube-ca-file <file>              the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify      if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>    server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>              bearer token used for authentication
# @option --kubeconfig <file>                path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>           path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>          path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>         path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
repo() {
    :;
}

# {{{ helm repo add
# @cmd add a chart repository
# @flag --allow-deprecated-repos             by default, this command will not allow adding official repos that have been permanently deleted.
# @option --ca-file <file>                   verify certificates of HTTPS-enabled servers using this CA bundle
# @option --cert-file <file>                 identify HTTPS client using this SSL certificate file
# @flag --force-update                       replace (overwrite) the repo if it already exists
# @flag -h --help                            help for add
# @flag --insecure-skip-tls-verify           skip tls certificate checks for the repository
# @option --key-file <file>                  identify HTTPS client using this SSL key file
# @flag --no-update                          Ignored.
# @flag --pass-credentials                   pass credentials to all domains
# @option --password <string>                chart repository password
# @flag --password-stdin                     read chart repository password from stdin
# @option --username <string>                chart repository username
# @option --burst-limit <int>                client-side default throttling limit (default 100)
# @flag --debug                              enable verbose output
# @option --kube-apiserver <string>          the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>          group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>            username to impersonate for the operation
# @option --kube-ca-file <file>              the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify      if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>    server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>              bearer token used for authentication
# @option --kubeconfig <file>                path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>           path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>          path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>         path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
# @arg name
# @arg url
repo::add() {
    :;
}
# }}} helm repo add

# {{{ helm repo index
# @cmd generate an index file given a directory containing packaged charts
# @flag -h --help                            help for index
# @flag --json                               output in JSON format
# @option --merge <string>                   merge the generated index into the given index
# @option --url <string>                     url of chart repository
# @option --burst-limit <int>                client-side default throttling limit (default 100)
# @flag --debug                              enable verbose output
# @option --kube-apiserver <string>          the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>          group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>            username to impersonate for the operation
# @option --kube-ca-file <file>              the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify      if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>    server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>              bearer token used for authentication
# @option --kubeconfig <file>                path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>           path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>          path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>         path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
# @arg dir
repo::index() {
    :;
}
# }}} helm repo index

# {{{ helm repo list
# @cmd list chart repositories
# @alias ls
# @flag -h --help                                  help for list
# @option -o --output[table|json|yaml] <format>    prints the output in the specified format.
# @option --burst-limit <int>                      client-side default throttling limit (default 100)
# @flag --debug                                    enable verbose output
# @option --kube-apiserver <string>                the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>                group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>                  username to impersonate for the operation
# @option --kube-ca-file <file>                    the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify            if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>          server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>                    bearer token used for authentication
# @option --kubeconfig <file>                      path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>                 path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>                path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>               path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
repo::list() {
    :;
}
# }}} helm repo list

# {{{ helm repo remove
# @cmd remove one or more chart repositories
# @alias rm
# @flag -h --help                            help for remove
# @option --burst-limit <int>                client-side default throttling limit (default 100)
# @flag --debug                              enable verbose output
# @option --kube-apiserver <string>          the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>          group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>            username to impersonate for the operation
# @option --kube-ca-file <file>              the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify      if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>    server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>              bearer token used for authentication
# @option --kubeconfig <file>                path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>           path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>          path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>         path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
# @arg name*[`_choice_repo_name`]
repo::remove() {
    :;
}
# }}} helm repo remove

# {{{ helm repo update
# @cmd update information of available charts locally from chart repositories
# @alias up
# @flag --fail-on-repo-update-fail           update fails if any of the repository updates fail
# @flag -h --help                            help for update
# @option --burst-limit <int>                client-side default throttling limit (default 100)
# @flag --debug                              enable verbose output
# @option --kube-apiserver <string>          the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>          group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>            username to impersonate for the operation
# @option --kube-ca-file <file>              the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify      if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>    server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>              bearer token used for authentication
# @option --kubeconfig <file>                path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>           path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>          path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>         path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
# @arg name*[`_choice_repo_name`]
repo::update() {
    :;
}
# }}} helm repo update
# }} helm repo

# {{ helm rollback
# @cmd roll back a release to a previous revision
# @flag --cleanup-on-fail                    allow deletion of new resources created in this rollback when rollback fails
# @flag --dry-run                            simulate a rollback
# @flag --force                              force resource update through delete/recreate if needed
# @flag -h --help                            help for rollback
# @option --history-max <int>                limit the maximum number of revisions saved per release.
# @flag --no-hooks                           prevent hooks from running during rollback
# @flag --recreate-pods                      performs pods restart for the resource if applicable
# @option --timeout <duration>               time to wait for any individual Kubernetes operation (like Jobs for hooks) (default 5m0s)
# @flag --wait                               if set, will wait until all Pods, PVCs, Services, and minimum number of Pods of a Deployment, StatefulSet, or ReplicaSet are in a ready state before marking the release as successful.
# @flag --wait-for-jobs                      if set and --wait enabled, will wait until all Jobs have been completed before marking the release as successful.
# @option --burst-limit <int>                client-side default throttling limit (default 100)
# @flag --debug                              enable verbose output
# @option --kube-apiserver <string>          the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>          group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>            username to impersonate for the operation
# @option --kube-ca-file <file>              the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify      if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>    server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>              bearer token used for authentication
# @option --kubeconfig <file>                path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>           path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>          path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>         path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
# @arg release![`_choice_release`]
# @arg revision[`_choice_revision`]
rollback() {
    :;
}
# }} helm rollback

# {{ helm search
# @cmd search for a keyword in charts
# @flag -h --help                            help for search
# @option --burst-limit <int>                client-side default throttling limit (default 100)
# @flag --debug                              enable verbose output
# @option --kube-apiserver <string>          the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>          group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>            username to impersonate for the operation
# @option --kube-ca-file <file>              the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify      if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>    server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>              bearer token used for authentication
# @option --kubeconfig <file>                path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>           path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>          path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>         path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
search() {
    :;
}

# {{{ helm search hub
# @cmd search for charts in the Artifact Hub or your own hub instance
# @option --endpoint <string>                      Hub instance to query for charts (default "https://hub.helm.sh")
# @flag --fail-on-no-result                        search fails if no results are found
# @flag -h --help                                  help for hub
# @flag --list-repo-url                            print charts repository URL
# @option --max-col-width <uint>                   maximum column width for output table (default 50)
# @option -o --output[table|json|yaml] <format>    prints the output in the specified format.
# @option --burst-limit <int>                      client-side default throttling limit (default 100)
# @flag --debug                                    enable verbose output
# @option --kube-apiserver <string>                the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>                group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>                  username to impersonate for the operation
# @option --kube-ca-file <file>                    the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify            if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>          server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>                    bearer token used for authentication
# @option --kubeconfig <file>                      path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>                 path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>                path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>               path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
# @arg keyword
search::hub() {
    :;
}
# }}} helm search hub

# {{{ helm search repo
# @cmd search repositories for a keyword in charts
# @flag --devel                                    use development versions (alpha, beta, and release candidate releases), too.
# @flag --fail-on-no-result                        search fails if no results are found
# @flag -h --help                                  help for repo
# @option --max-col-width <uint>                   maximum column width for output table (default 50)
# @option -o --output[table|json|yaml] <format>    prints the output in the specified format.
# @flag -r --regexp                                use regular expressions for searching repositories you have added
# @option --version <string>                       search using semantic versioning constraints on repositories you have added
# @flag -l --versions                              show the long listing, with each version of each chart on its own line, for repositories you have added
# @option --burst-limit <int>                      client-side default throttling limit (default 100)
# @flag --debug                                    enable verbose output
# @option --kube-apiserver <string>                the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>                group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>                  username to impersonate for the operation
# @option --kube-ca-file <file>                    the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify            if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>          server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>                    bearer token used for authentication
# @option --kubeconfig <file>                      path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>                 path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>                path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>               path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
# @arg keyword
search::repo() {
    :;
}
# }}} helm search repo
# }} helm search

# {{ helm show
# @cmd show information of a chart
# @alias inspect
# @flag -h --help                            help for show
# @option --burst-limit <int>                client-side default throttling limit (default 100)
# @flag --debug                              enable verbose output
# @option --kube-apiserver <string>          the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>          group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>            username to impersonate for the operation
# @option --kube-ca-file <file>              the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify      if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>    server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>              bearer token used for authentication
# @option --kubeconfig <file>                path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>           path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>          path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>         path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
show() {
    :;
}

# {{{ helm show all
# @cmd show all information of the chart
# @option --ca-file <file>                   verify certificates of HTTPS-enabled servers using this CA bundle
# @option --cert-file <file>                 identify HTTPS client using this SSL certificate file
# @flag --devel                              use development versions, too.
# @flag -h --help                            help for all
# @flag --insecure-skip-tls-verify           skip tls certificate checks for the chart download
# @option --key-file <file>                  identify HTTPS client using this SSL key file
# @option --keyring <file>                   location of public keys used for verification (default "/home/sigo/.gnupg/pubring.gpg")
# @flag --pass-credentials                   pass credentials to all domains
# @option --password <string>                chart repository password where to locate the requested chart
# @flag --plain-http                         use insecure HTTP connections for the chart download
# @option --repo <string>                    chart repository url where to locate the requested chart
# @option --username <string>                chart repository username where to locate the requested chart
# @flag --verify                             verify the package before using it
# @option --version <string>                 specify a version constraint for the chart version to use.
# @option --burst-limit <int>                client-side default throttling limit (default 100)
# @flag --debug                              enable verbose output
# @option --kube-apiserver <string>          the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>          group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>            username to impersonate for the operation
# @option --kube-ca-file <file>              the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify      if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>    server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>              bearer token used for authentication
# @option --kubeconfig <file>                path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>           path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>          path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>         path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
# @arg chart[`_choice_chart`]
show::all() {
    :;
}
# }}} helm show all

# {{{ helm show chart
# @cmd show the chart's definition
# @option --ca-file <file>                   verify certificates of HTTPS-enabled servers using this CA bundle
# @option --cert-file <file>                 identify HTTPS client using this SSL certificate file
# @flag --devel                              use development versions, too.
# @flag -h --help                            help for chart
# @flag --insecure-skip-tls-verify           skip tls certificate checks for the chart download
# @option --key-file <file>                  identify HTTPS client using this SSL key file
# @option --keyring <file>                   location of public keys used for verification (default "/home/sigo/.gnupg/pubring.gpg")
# @flag --pass-credentials                   pass credentials to all domains
# @option --password <string>                chart repository password where to locate the requested chart
# @flag --plain-http                         use insecure HTTP connections for the chart download
# @option --repo <string>                    chart repository url where to locate the requested chart
# @option --username <string>                chart repository username where to locate the requested chart
# @flag --verify                             verify the package before using it
# @option --version <string>                 specify a version constraint for the chart version to use.
# @option --burst-limit <int>                client-side default throttling limit (default 100)
# @flag --debug                              enable verbose output
# @option --kube-apiserver <string>          the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>          group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>            username to impersonate for the operation
# @option --kube-ca-file <file>              the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify      if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>    server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>              bearer token used for authentication
# @option --kubeconfig <file>                path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>           path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>          path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>         path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
# @arg chart[`_choice_chart`]
show::chart() {
    :;
}
# }}} helm show chart

# {{{ helm show crds
# @cmd show the chart's CRDs
# @option --ca-file <file>                   verify certificates of HTTPS-enabled servers using this CA bundle
# @option --cert-file <file>                 identify HTTPS client using this SSL certificate file
# @flag --devel                              use development versions, too.
# @flag -h --help                            help for crds
# @flag --insecure-skip-tls-verify           skip tls certificate checks for the chart download
# @option --key-file <file>                  identify HTTPS client using this SSL key file
# @option --keyring <file>                   location of public keys used for verification (default "/home/sigo/.gnupg/pubring.gpg")
# @flag --pass-credentials                   pass credentials to all domains
# @option --password <string>                chart repository password where to locate the requested chart
# @flag --plain-http                         use insecure HTTP connections for the chart download
# @option --repo <string>                    chart repository url where to locate the requested chart
# @option --username <string>                chart repository username where to locate the requested chart
# @flag --verify                             verify the package before using it
# @option --version <string>                 specify a version constraint for the chart version to use.
# @option --burst-limit <int>                client-side default throttling limit (default 100)
# @flag --debug                              enable verbose output
# @option --kube-apiserver <string>          the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>          group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>            username to impersonate for the operation
# @option --kube-ca-file <file>              the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify      if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>    server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>              bearer token used for authentication
# @option --kubeconfig <file>                path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>           path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>          path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>         path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
# @arg chart[`_choice_chart`]
show::crds() {
    :;
}
# }}} helm show crds

# {{{ helm show readme
# @cmd show the chart's README
# @option --ca-file <file>                   verify certificates of HTTPS-enabled servers using this CA bundle
# @option --cert-file <file>                 identify HTTPS client using this SSL certificate file
# @flag --devel                              use development versions, too.
# @flag -h --help                            help for readme
# @flag --insecure-skip-tls-verify           skip tls certificate checks for the chart download
# @option --key-file <file>                  identify HTTPS client using this SSL key file
# @option --keyring <file>                   location of public keys used for verification (default "/home/sigo/.gnupg/pubring.gpg")
# @flag --pass-credentials                   pass credentials to all domains
# @option --password <string>                chart repository password where to locate the requested chart
# @flag --plain-http                         use insecure HTTP connections for the chart download
# @option --repo <string>                    chart repository url where to locate the requested chart
# @option --username <string>                chart repository username where to locate the requested chart
# @flag --verify                             verify the package before using it
# @option --version <string>                 specify a version constraint for the chart version to use.
# @option --burst-limit <int>                client-side default throttling limit (default 100)
# @flag --debug                              enable verbose output
# @option --kube-apiserver <string>          the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>          group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>            username to impersonate for the operation
# @option --kube-ca-file <file>              the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify      if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>    server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>              bearer token used for authentication
# @option --kubeconfig <file>                path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>           path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>          path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>         path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
# @arg chart[`_choice_chart`]
show::readme() {
    :;
}
# }}} helm show readme

# {{{ helm show values
# @cmd show the chart's values
# @option --ca-file <file>                   verify certificates of HTTPS-enabled servers using this CA bundle
# @option --cert-file <file>                 identify HTTPS client using this SSL certificate file
# @flag --devel                              use development versions, too.
# @flag -h --help                            help for values
# @flag --insecure-skip-tls-verify           skip tls certificate checks for the chart download
# @option --jsonpath <path>                  supply a JSONPath expression to filter the output
# @option --key-file <file>                  identify HTTPS client using this SSL key file
# @option --keyring <file>                   location of public keys used for verification (default "/home/sigo/.gnupg/pubring.gpg")
# @flag --pass-credentials                   pass credentials to all domains
# @option --password <string>                chart repository password where to locate the requested chart
# @flag --plain-http                         use insecure HTTP connections for the chart download
# @option --repo <string>                    chart repository url where to locate the requested chart
# @option --username <string>                chart repository username where to locate the requested chart
# @flag --verify                             verify the package before using it
# @option --version <string>                 specify a version constraint for the chart version to use.
# @option --burst-limit <int>                client-side default throttling limit (default 100)
# @flag --debug                              enable verbose output
# @option --kube-apiserver <string>          the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>          group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>            username to impersonate for the operation
# @option --kube-ca-file <file>              the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify      if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>    server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>              bearer token used for authentication
# @option --kubeconfig <file>                path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>           path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>          path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>         path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
# @arg chart[`_choice_chart`]
show::values() {
    :;
}
# }}} helm show values
# }} helm show

# {{ helm status
# @cmd display the status of the named release
# @flag -h --help                                  help for status
# @option -o --output[table|json|yaml] <format>    prints the output in the specified format.
# @option --revision <int>                         if set, display the status of the named release with revision
# @flag --show-desc                                if set, display the description message of the named release
# @flag --show-resources                           if set, display the resources of the named release
# @option --burst-limit <int>                      client-side default throttling limit (default 100)
# @flag --debug                                    enable verbose output
# @option --kube-apiserver <string>                the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>                group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>                  username to impersonate for the operation
# @option --kube-ca-file <file>                    the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify            if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>          server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>                    bearer token used for authentication
# @option --kubeconfig <file>                      path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>                 path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>                path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>               path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
# @arg release_name[`_choice_release`]
status() {
    :;
}
# }} helm status

# {{ helm template
# @cmd locally render templates
# @option -a --api-versions* <string>        Kubernetes api versions used for Capabilities.APIVersions
# @flag --atomic                             if set, the installation process deletes the installation on failure.
# @option --ca-file <file>                   verify certificates of HTTPS-enabled servers using this CA bundle
# @option --cert-file <file>                 identify HTTPS client using this SSL certificate file
# @flag --create-namespace                   create the release namespace if not present
# @flag --dependency-update                  update dependencies if they are missing before installing the chart
# @option --description <string>             add a custom description
# @flag --devel                              use development versions, too.
# @flag --disable-openapi-validation         if set, the installation process will not validate rendered templates against the Kubernetes OpenAPI Schema
# @option --dry-run <string[="client"]>      simulate an install.
# @flag --enable-dns                         enable DNS lookups when rendering templates
# @flag --force                              force resource updates through a replacement strategy
# @flag -g --generate-name                   generate the name (and omit the NAME parameter)
# @flag -h --help                            help for template
# @flag --include-crds                       include CRDs in the templated output
# @flag --insecure-skip-tls-verify           skip tls certificate checks for the chart download
# @flag --is-upgrade                         set .Release.IsUpgrade instead of .Release.IsInstall
# @option --key-file <file>                  identify HTTPS client using this SSL key file
# @option --keyring <file>                   location of public keys used for verification (default "/home/sigo/.gnupg/pubring.gpg")
# @option --kube-version <string>            Kubernetes version used for Capabilities.KubeVersion
# @option -l --labels <stringToString>       Labels that would be added to release metadata.
# @option --name-template <string>           specify template used to name the release
# @flag --no-hooks                           prevent hooks from running during install
# @option --output-dir <path>                writes the executed templates to files in output-dir instead of stdout
# @flag --pass-credentials                   pass credentials to all domains
# @option --password <string>                chart repository password where to locate the requested chart
# @flag --plain-http                         use insecure HTTP connections for the chart download
# @option --post-renderer <path>             the path to an executable to be used for post rendering.
# @option --post-renderer-args <postRendererArgsSlice>  an argument to the post-renderer (can specify multiple) (default [])
# @flag --release-name                       use release name in the output-dir path.
# @flag --render-subchart-notes              if set, render subchart notes along with the parent
# @flag --replace                            re-use the given name, only if that name is a deleted release which remains in the history.
# @option --repo <string>                    chart repository url where to locate the requested chart
# @option --set* <string>                    set values on the command line (can specify multiple or separate values with commas: key1=val1,key2=val2)
# @option --set-file*,[`_choice_set_file`] <file>  set values from respective files specified via the command line (can specify multiple or separate values with commas: key1=path1,key2=path2)
# @option --set-json* <string>               set JSON values on the command line (can specify multiple or separate values with commas: key1=jsonval1,key2=jsonval2)
# @option --set-literal* <string>            set a literal STRING value on the command line
# @option --set-string* <string>             set STRING values on the command line (can specify multiple or separate values with commas: key1=val1,key2=val2)
# @option -s --show-only* <string>           only show manifests rendered from the given templates
# @flag --skip-crds                          if set, no CRDs will be installed.
# @flag --skip-tests                         skip tests from templated output
# @option --timeout <duration>               time to wait for any individual Kubernetes operation (like Jobs for hooks) (default 5m0s)
# @option --username <string>                chart repository username where to locate the requested chart
# @flag --validate                           validate your manifests against the Kubernetes cluster you are currently pointing at.
# @option -f --values* <file>                specify values in a YAML file or a URL (can specify multiple)
# @flag --verify                             verify the package before using it
# @option --version <string>                 specify a version constraint for the chart version to use.
# @flag --wait                               if set, will wait until all Pods, PVCs, Services, and minimum number of Pods of a Deployment, StatefulSet, or ReplicaSet are in a ready state before marking the release as successful.
# @flag --wait-for-jobs                      if set and --wait enabled, will wait until all Jobs have been completed before marking the release as successful.
# @option --burst-limit <int>                client-side default throttling limit (default 100)
# @flag --debug                              enable verbose output
# @option --kube-apiserver <string>          the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>          group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>            username to impersonate for the operation
# @option --kube-ca-file <file>              the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify      if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>    server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>              bearer token used for authentication
# @option --kubeconfig <file>                path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>           path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>          path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>         path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
# @arg name
# @arg chart[`_choice_chart`]
template() {
    :;
}
# }} helm template

# {{ helm test
# @cmd run tests for a release
# @option --filter* <string>                 specify tests by attribute (currently "name") using attribute=value syntax or '!attribute=value' to exclude a test (can specify multiple or separate values with commas: name=test1,name=test2)
# @flag -h --help                            help for test
# @flag --logs                               dump the logs from test pods (this runs after all tests are complete, but before any cleanup)
# @option --timeout <duration>               time to wait for any individual Kubernetes operation (like Jobs for hooks) (default 5m0s)
# @option --burst-limit <int>                client-side default throttling limit (default 100)
# @flag --debug                              enable verbose output
# @option --kube-apiserver <string>          the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>          group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>            username to impersonate for the operation
# @option --kube-ca-file <file>              the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify      if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>    server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>              bearer token used for authentication
# @option --kubeconfig <file>                path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>           path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>          path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>         path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
# @arg release[`_choice_release`]
test() {
    :;
}
# }} helm test

# {{ helm uninstall
# @cmd uninstall a release
# @alias del,delete,un
# @option --cascade[background|orphan|foreground] <string>  Must be "background", "orphan", or "foreground".
# @option --description <string>             add a custom description
# @flag --dry-run                            simulate a uninstall
# @flag -h --help                            help for uninstall
# @flag --ignore-not-found                   Treat "release not found" as a successful uninstall
# @flag --keep-history                       remove all associated resources and mark the release as deleted, but retain the release history
# @flag --no-hooks                           prevent hooks from running during uninstallation
# @option --timeout <duration>               time to wait for any individual Kubernetes operation (like Jobs for hooks) (default 5m0s)
# @flag --wait                               if set, will wait until all the resources are deleted before returning.
# @option --burst-limit <int>                client-side default throttling limit (default 100)
# @flag --debug                              enable verbose output
# @option --kube-apiserver <string>          the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>          group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>            username to impersonate for the operation
# @option --kube-ca-file <file>              the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify      if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>    server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>              bearer token used for authentication
# @option --kubeconfig <file>                path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>           path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>          path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>         path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
# @arg release_name[`_choice_release`]
uninstall() {
    :;
}
# }} helm uninstall

# {{ helm upgrade
# @cmd upgrade a release
# @flag --atomic                                   if set, upgrade process rolls back changes made in case of failed upgrade.
# @option --ca-file <file>                         verify certificates of HTTPS-enabled servers using this CA bundle
# @option --cert-file <file>                       identify HTTPS client using this SSL certificate file
# @flag --cleanup-on-fail                          allow deletion of new resources created in this upgrade when upgrade fails
# @flag --create-namespace                         if --install is set, create the release namespace if not present
# @flag --dependency-update                        update dependencies if they are missing before installing the chart
# @option --description <string>                   add a custom description
# @flag --devel                                    use development versions, too.
# @flag --disable-openapi-validation               if set, the upgrade process will not validate rendered templates against the Kubernetes OpenAPI Schema
# @option --dry-run <string[="client"]>            simulate an install.
# @flag --enable-dns                               enable DNS lookups when rendering templates
# @flag --force                                    force resource updates through a replacement strategy
# @flag -h --help                                  help for upgrade
# @option --history-max <int>                      limit the maximum number of revisions saved per release.
# @flag --insecure-skip-tls-verify                 skip tls certificate checks for the chart download
# @flag -i --install                               if a release by this name doesn't already exist, run an install
# @option --key-file <file>                        identify HTTPS client using this SSL key file
# @option --keyring <file>                         location of public keys used for verification (default "/home/sigo/.gnupg/pubring.gpg")
# @option -l --labels <stringToString>             Labels that would be added to release metadata.
# @flag --no-hooks                                 disable pre/post upgrade hooks
# @option -o --output[table|json|yaml] <format>    prints the output in the specified format.
# @flag --pass-credentials                         pass credentials to all domains
# @option --password <string>                      chart repository password where to locate the requested chart
# @flag --plain-http                               use insecure HTTP connections for the chart download
# @option --post-renderer <path>                   the path to an executable to be used for post rendering.
# @option --post-renderer-args <postRendererArgsSlice>  an argument to the post-renderer (can specify multiple) (default [])
# @flag --render-subchart-notes                    if set, render subchart notes along with the parent
# @option --repo <string>                          chart repository url where to locate the requested chart
# @flag --reset-then-reuse-values                  when upgrading, reset the values to the ones built into the chart, apply the last release's values and merge in any overrides from the command line via --set and -f.
# @flag --reset-values                             when upgrading, reset the values to the ones built into the chart
# @flag --reuse-values                             when upgrading, reuse the last release's values and merge in any overrides from the command line via --set and -f.
# @option --set* <string>                          set values on the command line (can specify multiple or separate values with commas: key1=val1,key2=val2)
# @option --set-file*,[`_choice_set_file`] <file>  set values from respective files specified via the command line (can specify multiple or separate values with commas: key1=path1,key2=path2)
# @option --set-json* <string>                     set JSON values on the command line (can specify multiple or separate values with commas: key1=jsonval1,key2=jsonval2)
# @option --set-literal* <string>                  set a literal STRING value on the command line
# @option --set-string* <string>                   set STRING values on the command line (can specify multiple or separate values with commas: key1=val1,key2=val2)
# @flag --skip-crds                                if set, no CRDs will be installed when an upgrade is performed with install flag enabled.
# @option --timeout <duration>                     time to wait for any individual Kubernetes operation (like Jobs for hooks) (default 5m0s)
# @option --username <string>                      chart repository username where to locate the requested chart
# @option -f --values* <file>                      specify values in a YAML file or a URL (can specify multiple)
# @flag --verify                                   verify the package before using it
# @option --version <string>                       specify a version constraint for the chart version to use.
# @flag --wait                                     if set, will wait until all Pods, PVCs, Services, and minimum number of Pods of a Deployment, StatefulSet, or ReplicaSet are in a ready state before marking the release as successful.
# @flag --wait-for-jobs                            if set and --wait enabled, will wait until all Jobs have been completed before marking the release as successful.
# @option --burst-limit <int>                      client-side default throttling limit (default 100)
# @flag --debug                                    enable verbose output
# @option --kube-apiserver <string>                the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>                group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>                  username to impersonate for the operation
# @option --kube-ca-file <file>                    the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify            if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>          server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>                    bearer token used for authentication
# @option --kubeconfig <file>                      path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>                 path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>                path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>               path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
# @arg release[`_choice_release`]
# @arg chart[`_choice_chart`]
upgrade() {
    :;
}
# }} helm upgrade

# {{ helm verify
# @cmd verify that a chart at the given path has been signed and is valid
# @flag -h --help                            help for verify
# @option --keyring <file>                   keyring containing public keys (default "/home/sigo/.gnupg/pubring.gpg")
# @option --burst-limit <int>                client-side default throttling limit (default 100)
# @flag --debug                              enable verbose output
# @option --kube-apiserver <string>          the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>          group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>            username to impersonate for the operation
# @option --kube-ca-file <file>              the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify      if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>    server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>              bearer token used for authentication
# @option --kubeconfig <file>                path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>           path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>          path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>         path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
# @arg path
verify() {
    :;
}
# }} helm verify

# {{ helm version
# @cmd print the client version information
# @flag -h --help                            help for version
# @flag --short                              print the version number
# @option --template <string>                template for version string format
# @option --burst-limit <int>                client-side default throttling limit (default 100)
# @flag --debug                              enable verbose output
# @option --kube-apiserver <string>          the address and the port for the Kubernetes API server
# @option --kube-as-group* <string>          group to impersonate for the operation, this flag can be repeated to specify multiple groups.
# @option --kube-as-user <string>            username to impersonate for the operation
# @option --kube-ca-file <file>              the certificate authority file for the Kubernetes API server connection
# @option --kube-context[`_choice_kube_context`] <string>  name of the kubeconfig context to use
# @flag --kube-insecure-skip-tls-verify      if true, the Kubernetes API server's certificate will not be checked for validity.
# @option --kube-tls-server-name <string>    server name to use for Kubernetes API server certificate validation.
# @option --kube-token <string>              bearer token used for authentication
# @option --kubeconfig <file>                path to the kubeconfig file
# @option -n --namespace[`_choice_kube_namespace`] <string>  namespace scope for this request
# @option --registry-config <file>           path to the registry config file (default "/home/sigo/.config/helm/registry/config.json")
# @option --repository-cache <file>          path to the file containing cached repository indexes (default "/home/sigo/.cache/helm/repository")
# @option --repository-config <file>         path to the file containing repository names and URLs (default "/home/sigo/.config/helm/repositories.yaml")
version() {
    :;
}
# }} helm version

. "$ARGC_COMPLETIONS_ROOT/utils/_argc_utils.sh"

_choice_kube_context() {
    kubectl config get-contexts -o name
}

_choice_kube_namespace() {
    kubectl get namespaces | tail -n +2 | gawk '{print $1}'
}

_choice_chart() {
    _choice_repo_chart
    _argc_util_comp_path isdir
}

_choice_release() {
    _helm list --output json | yq '.[] | .name + "	" + .chart'
}

_choice_set_file() {
    _argc_util_mode_kv =
    if [[ -z "$argc__kv_prefix" ]]; then
        :;
    else
        _argc_util_comp_path
    fi
}

_choice_plugin() {
    _helm plugin list | _argc_util_transform_table 'NAME;DESCRIPTION' '\t'
}

_choice_repo_chart() {
    _argc_util_mode_kv /
    if [[ -z "$argc__kv_prefix" ]]; then
        _choice_repo_name | _argc_util_transform suffix=/ nospace
    else
        chart_yaml_path="$(_argc_util_path_resolve CACHE_DIR /helm/repository/${argc__kv_key}-index.yaml)"
        if [[ ! -f  "$chart_yaml_path" ]]; then
            return
        fi
        cat "$chart_yaml_path" | \
        yq '.entries |
            to_entries | .[] |
            {"name": .key, "decription": .value[0].description } |
            .decription |= split("\n") |
            .name + "	" + .decription[0]'
    fi
}

_choice_repo_name() {
    repo_yaml_path="$(_argc_util_path_resolve CONFIG_DIR helm/repositories.yaml)" 
    if [[ ! -f  "$repo_yaml_path" ]]; then
        return
    fi
    cat "$repo_yaml_path" | yq '.repositories[] | .name + "	" + .url'
}

_choice_revision() {
    _helm history --output json "${argc__positionals[0]}" | yq '.[] | .revision + "	" + .description'
}

_helm() {
    local opts=( \
        --kube-apiserver \
        --kube-as-group \
        --kube-as-user \
        --kube-ca-file \
        --kube-context \
        --kube-insecure-skip-tls-verify \
        --kube-tls-server-name \
        --kube-token \
        --kubeconfig \
        --namespace \
        --registry-config \
        --repository-cache \
        --repository-config \
    )
    helm $(_argc_util_param_select_options ${opts[@]}) "$@"
}

command eval "$(argc --argc-eval "$0" "$@")"