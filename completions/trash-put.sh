#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -h --help                              show this help message and exit
# @option --print-completion[bash|zsh|tcsh]    print shell completion script
# @flag -d --directory                         ignored (for GNU rm compatibility)
# @flag -f --force                             silently ignore nonexistent files
# @flag -i --interactive                       prompt before every removal
# @flag -r                                     ignored (for GNU rm compatibility)
# @flag -R                                     ignored (for GNU rm compatibility)
# @flag --recursive                            ignored (for GNU rm compatibility)
# @option --trash-dir <TRASHDIR>               use TRASHDIR as trash folder
# @flag -v --verbose                           explain what is being done
# @flag --version                              show program's version number and exit
# @arg files*

command eval "$(argc --argc-eval "$0" "$@")"