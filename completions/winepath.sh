#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -u --unix       converts a Windows path to a Unix path
# @flag -w --windows    converts a Unix path to a long Windows path
# @flag -l --long       converts the short Windows path of an existing file or directory to the long format
# @flag -s --short      converts the long Windows path of an existing file or directory to the short format
# @flag -0              separate output with \0 character, instead of a newline
# @flag -h --help       output this help message and exit
# @arg path*

command eval "$(argc --argc-eval "$0" "$@")"