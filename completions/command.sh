#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -p    use a default value for PATH that is guaranteed to find all of the standard utilities
# @flag -v    print a description of COMMAND similar to the `type' builtin
# @flag -V    print a more verbose description of each COMMAND

command eval "$(argc --argc-eval "$0" "$@")"