#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -p    if MODE is omitted, output in a form that may be reused as input
# @flag -S    makes the output symbolic; otherwise an octal number is output

command eval "$(argc --argc-eval "$0" "$@")"