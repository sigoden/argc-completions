#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -l    lists process IDs in addition to the normal information
# @flag -n    lists only processes that have changed status since the last notification
# @flag -p    lists process IDs only
# @flag -r    restrict output to running jobs
# @flag -s    restrict output to stopped jobs

command eval "$(argc --argc-eval "$0" "$@")"