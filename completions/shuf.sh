#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -e --echo                     treat each ARG as an input line
# @option -i --input-range <LO-HI>    treat each number LO through HI as an input line
# @option -n --head-count <COUNT>     output at most COUNT lines
# @option -o --output <FILE>          write result to FILE instead of standard output
# @option --random-source <FILE>      get random bytes from FILE
# @flag -r --repeat                   output lines can be repeated
# @flag -z --zero-terminated          line delimiter is NUL, not newline
# @flag --help                        display this help and exit
# @flag --version                     output version information and exit
# @arg file

command eval "$(argc --argc-eval "$0" "$@")"