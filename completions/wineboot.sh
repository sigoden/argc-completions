#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -h --help           Display this help message
# @flag -e --end-session    End the current session cleanly
# @flag -f --force          Force exit for processes that don't exit cleanly
# @flag -i --init           Perform initialization for first Wine instance
# @flag -k --kill           Kill running processes without any cleanup
# @flag -r --restart        Restart only, don't do normal startup operations
# @flag -s --shutdown       Shutdown only, don't reboot
# @flag -u --update         Update the wineprefix directory

command eval "$(argc --argc-eval "$0" "$@")"